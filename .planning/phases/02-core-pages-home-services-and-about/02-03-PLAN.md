---
phase: 02-core-pages-home-services-and-about
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/content/services.ts
  - src/components/sections/TrustBadges.tsx
  - src/app/(marketing)/services/page.tsx
  - src/app/(marketing)/services/[slug]/page.tsx
autonomous: true

must_haves:
  truths:
    - "Visitor can navigate to a dedicated page for each of the 7 services (New Builds, Renovations, Extensions, Conversions, Basements, Refurbishments, Barn Conversions)"
    - "Each service page shows relevant warranty/trust content specific to that service (e.g. JCT + 10yr warranty for New Builds, 3yr workmanship guarantee for Extensions)"
    - "Visitor can browse a services index page linking to all 7 individual service pages"
  artifacts:
    - path: "src/content/services.ts"
      provides: "Typed source-of-truth array of all 7 services with slug, name, summary, description, and per-service warranty content"
      contains: "export const SERVICES"
    - path: "src/components/sections/TrustBadges.tsx"
      provides: "Reusable warranty/credential badge display, content-agnostic to which service's warranty it renders"
      min_lines: 15
    - path: "src/app/(marketing)/services/[slug]/page.tsx"
      provides: "Single dynamic template rendering any of the 7 services via generateStaticParams, guaranteeing structural consistency of trust content across all 7"
      contains: "generateStaticParams"
    - path: "src/app/(marketing)/services/page.tsx"
      provides: "Services index page linking to all 7 individual service pages"
      min_lines: 15
  key_links:
    - from: "src/app/(marketing)/services/[slug]/page.tsx"
      to: "src/content/services.ts"
      via: "import { SERVICES } from \"@/content/services\""
      pattern: "from \"@/content/services\""
    - from: "src/app/(marketing)/services/[slug]/page.tsx"
      to: "src/components/sections/TrustBadges.tsx"
      via: "render <TrustBadges warranty={service.warranty} />"
      pattern: "TrustBadges"
    - from: "src/app/(marketing)/services/page.tsx"
      to: "src/app/(marketing)/services/[slug]/page.tsx"
      via: "next/link to /services/[slug] for each SERVICES entry"
      pattern: "services/\\$\\{"
---

<objective>
Build all 7 service pages (SERV-01) with per-service warranty/trust content (SERV-02) from a single typed content array and one dynamic route template, plus an index page linking to all 7.

Purpose: Per 02-RESEARCH.md, hand-building 7 separate `page.tsx` files is the primary risk to SERV-02's consistency (a warranty block silently missing on one page). A single `content/services.ts` + `[slug]/page.tsx` + `generateStaticParams` pattern makes a missing warranty field a type error, not a shipped gap, and mirrors the project's "hardcoded content, not CMS" decision at the data layer.
Output: `content/services.ts` (7 real services with real warranty copy from PROJECT.md/REQUIREMENTS.md), `components/sections/TrustBadges.tsx`, `services/page.tsx` index, `services/[slug]/page.tsx` dynamic template — all 7 slugs statically generated at build time.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-core-pages-home-services-and-about/02-RESEARCH.md
@src/components/ui/Typography.tsx
@src/components/ui/Button.tsx
@src/components/motion/Reveal.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Typed services content + TrustBadges component</name>
  <files>src/content/services.ts, src/components/sections/TrustBadges.tsx</files>
  <action>
Create `src/content/services.ts` exporting a `Service` type and `SERVICES: Service[]` array with exactly 7 entries, per 02-RESEARCH.md Pattern 4:

```typescript
export type Service = {
  slug: string;
  name: string;
  summary: string;      // one-line, used on the index page
  description: string;  // longer paragraph(s), used on the detail page
  warranty: { label: string; detail: string };
};
```

Populate all 7 real services referenced in REQUIREMENTS.md SERV-01/SERV-02 and PROJECT.md: New Builds, Renovations, Extensions, Conversions (mention HMOs, commercial/residential flats, loft, garage in its description), Basements, Refurbishments, Barn Conversions. Use real warranty content already established in REQUIREMENTS.md: New Builds -> "JCT Contract + 10-Year Structural Warranty"; Extensions -> "3-Year Workmanship Guarantee". For the remaining 5 services without an explicitly specified distinct warranty in REQUIREMENTS.md, apply the same "3-Year Workmanship Guarantee" baseline (this is the company's standard guarantee per TRUST-02) unless PROJECT.md specifies otherwise — do not fabricate certifications not evidenced in project docs.

Create `src/components/sections/TrustBadges.tsx`: a small, content-agnostic (within the `Service["warranty"]` shape) component accepting `{ warranty: Service["warranty"] }` (or a generic `{ label: string; detail: string }` shape to avoid a hard content-layer import, planner's choice — prefer the generic shape for reuse), rendering the label prominently (`Typography variant="display-sm"` or similar) and detail as `body-sm`, styled as a bordered badge/card using semantic tokens (`border-line`, `bg-paper-dim`) — not buried, not decorative-only.
  </action>
  <verify>`npx tsc --noEmit` passes. Import `SERVICES` in a scratch check confirms exactly 7 entries with unique slugs and every entry has a non-empty `warranty.label`.</verify>
  <done>`content/services.ts` has 7 real, distinct services each with real warranty copy (no placeholder "Lorem ipsum" or fabricated certifications); `TrustBadges` renders any warranty object prominently using only semantic design tokens.</done>
</task>

<task type="auto">
  <name>Task 2: Services index + dynamic [slug] detail template</name>
  <files>src/app/(marketing)/services/page.tsx, src/app/(marketing)/services/[slug]/page.tsx</files>
  <action>
Create `src/app/(marketing)/services/page.tsx`: a server component listing all 7 `SERVICES` entries (name + summary) as `next/link` cards/list items linking to `/services/${slug}`, with a `TextReveal as="h1"` page heading (e.g. "Our Services").

Create `src/app/(marketing)/services/[slug]/page.tsx` per 02-RESEARCH.md Pattern 4:
- `export function generateStaticParams()` returning all 7 `{ slug }` params from `SERVICES`.
- `params: Promise<{ slug: string }>` (Next.js 15 async params), `await params`, find the matching service, call `notFound()` from `next/navigation` if no match.
- Render: `TextReveal as="h1"` with `service.name`, `Reveal`-wrapped `service.description`, and `<TrustBadges warranty={service.warranty} />` positioned prominently (not below a fold-worthy amount of unrelated content) to satisfy SERV-02.
- Optionally add `generateMetadata` for a per-service `<title>` (nice-to-have, not required by this phase's requirements — skip if time-constrained, ENG-03 SEO hardening is Phase 5 scope).
  </action>
  <verify>`npm run build` succeeds and lists all 7 `/services/[slug]` routes as static (○, not λ) in the build output. Navigate to `/services` and each of the 7 detail URLs in dev server — each renders real name/description/warranty content, an invalid slug (e.g. `/services/nonexistent`) renders the Next.js not-found page.</verify>
  <done>All 7 service pages are statically generated, reachable from the services index, each displays its own real warranty/trust content via the shared `TrustBadges` component, and no two service pages have divergent trust-content structure.</done>
</task>

</tasks>

<verification>
`npm run build` output lists `/services` and all 7 `/services/[slug]` routes as static. View-source on two different service pages confirms both have a `TrustBadges` block present in SSR HTML with real (not identical/copy-pasted-incorrectly) warranty text.
</verification>

<success_criteria>
SERV-01 (7 dedicated service pages) and SERV-02 (per-service warranty/trust content) are both demonstrably true, driven entirely from one typed content array and one route template — no per-service structural drift possible.
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-pages-home-services-and-about/02-03-SUMMARY.md`
</output>
