---
phase: 02-core-pages-home-services-and-about
plan: 14
type: execute
wave: 2
depends_on: ["02-11"]
files_modified:
  - src/app/(marketing)/services/page.tsx
  - src/app/(marketing)/services/[slug]/page.tsx
  - src/components/sections/ServicesSection.tsx
  - next.config.ts
  - public/images/services/new-builds.jpg
  - public/images/services/renovations.jpg
  - public/images/services/extensions.jpg
  - public/images/services/conversions.jpg
  - public/images/services/basements.jpg
  - public/images/services/refurbishments.jpg
  - public/images/services/barn-conversions.jpg

autonomous: true

must_haves:
  truths:
    - "Visiting /services renders a real dedicated page listing all 7 services, not a redirect to /#services"
    - "Visiting /services/[slug] for any of the 7 service slugs renders full per-service depth (description + full warranty detail), not just the condensed summary"
    - "The homepage Services section is a 3-column photo-led teaser (image + name + summary + warranty label per service) that links out to /services"
  artifacts:
    - path: "src/app/(marketing)/services/page.tsx"
      provides: "Services index page listing all 7 services, linking to /services/[slug]"
      contains: "SERVICES.map"
    - path: "src/app/(marketing)/services/[slug]/page.tsx"
      provides: "Per-service detail page: generateStaticParams, full description + TrustBadges"
      contains: "generateStaticParams"
    - path: "src/components/sections/ServicesSection.tsx"
      provides: "3-column photo-led homepage teaser linking to /services"
      contains: "href=\"/services\""
  key_links:
    - from: "src/app/(marketing)/services/[slug]/page.tsx"
      to: "src/content/services.ts"
      via: "generateStaticParams() returning SERVICES.map((s) => ({ slug: s.slug }))"
      pattern: "generateStaticParams"
    - from: "next.config.ts"
      to: "src/app/(marketing)/services/page.tsx"
      via: "absence of a /services redirect entry in redirects()"
      pattern: "redirects"
---

<objective>
Reintroduce `/services` as a real dedicated route (recovered from git history commit `b23826b`, dropped in `85b62f1`) carrying full per-service depth, and rework the homepage's `ServicesSection` from a minimal card-teaser into a 3-column photo-led teaser that links out to it — per `02-CONTEXT.md`'s hybrid page-structure decision.

Purpose: The original single-page pivot condensed Services into the homepage only; the second pivot restores full per-service depth as its own route (matching Storey's 3-column photo-led Services presentation) while keeping the homepage teaser as an entry point, not a replacement. The exact prior route implementation is recoverable from git history, so this is a revert-and-extend, not new architecture.
Output: `app/(marketing)/services/page.tsx` and `app/(marketing)/services/[slug]/page.tsx` restored and building cleanly against current `content/services.ts`; the `/services` → `/#services` redirect removed from `next.config.ts` (the `/about` → `/#about` redirect stays, since About remains homepage-only); `ServicesSection.tsx` reworked into a 3-column photo-led grid; 7 self-hosted stock images for the 7 services.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-core-pages-home-services-and-about/02-CONTEXT.md
@.planning/phases/02-core-pages-home-services-and-about/02-RESEARCH.md
@src/content/services.ts
@src/components/sections/ServicesSection.tsx
@src/components/sections/TrustBadges.tsx
@next.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Recover /services routes from git history and remove the stale redirect</name>
  <files>src/app/(marketing)/services/page.tsx, src/app/(marketing)/services/[slug]/page.tsx, next.config.ts</files>
  <action>
Recover the two files dropped in commit `85b62f1` from their last-known-good state at commit `b23826b`:
- `git show b23826b:"src/app/(marketing)/services/page.tsx"` → write to `src/app/(marketing)/services/page.tsx`
- `git show b23826b:"src/app/(marketing)/services/[slug]/page.tsx"` → write to `src/app/(marketing)/services/[slug]/page.tsx`

Adapt both against the CURRENT codebase state (do not assume nothing changed since `b23826b`):
- Confirm `content/services.ts`'s `Service` type shape (`slug`, `name`, `summary`, `description`, `warranty: { label, detail }`) still matches what these files expect — it does as of this plan's research, but re-verify directly before writing.
- Confirm `TrustBadges` (`src/components/sections/TrustBadges.tsx`) still accepts `{ warranty, className }` — it does; no changes needed to `TrustBadges` itself.
- Confirm the recovered files' imports (`TextReveal`, `Reveal`, `Typography`, `next/link`, `next/navigation`'s `notFound`) all still resolve at their current paths.

Remove the `{ source: "/services", destination: "/#services", permanent: true }` entry from `next.config.ts`'s `redirects()` array — **this is required**, without it the reintroduced `/services` route is permanently unreachable (redirects evaluate before filesystem route resolution, per `02-RESEARCH.md` Pitfall 4). Leave the `/work` → `/projects` and `/about` → `/#about` entries untouched.
  </action>
  <verify>`npm run build` succeeds and the build output's route list includes `/services` and `/services/[slug]` as static routes (7 generated params). `grep -n "services" next.config.ts` shows no `/services` redirect entry remains. `curl -I http://localhost:3000/services` (dev server) returns 200, not a 308 redirect.</verify>
  <done>`/services` renders a real index page listing all 7 services; `/services/[slug]` renders full per-service depth for each of the 7 slugs; the stale `/services` → `/#services` redirect no longer exists in `next.config.ts`.</done>
</task>

<task type="auto">
  <name>Task 2: Rework ServicesSection into a 3-column photo-led homepage teaser</name>
  <files>src/components/sections/ServicesSection.tsx, public/images/services/new-builds.jpg, public/images/services/renovations.jpg, public/images/services/extensions.jpg, public/images/services/conversions.jpg, public/images/services/basements.jpg, public/images/services/refurbishments.jpg, public/images/services/barn-conversions.jpg</files>
  <action>
**Source 7 self-hosted stock images**, one representative photo per service (matching each service's real nature — e.g. a new-build exterior for "New Builds", a kitchen/bathroom for "Renovations", a loft/garage for "Conversions" — reasonably matched, not randomly assigned), saved as `public/images/services/{slug}.jpg` matching each service's `slug` field exactly. Same sourcing approach as `02-13`'s Task 1 (free/Unsplash-License UK residential-construction imagery, self-hosted, ~1200px wide is sufficient for a grid card) — if network access is unavailable, generate 7 placeholder JPGs and flag this clearly in the SUMMARY as needing a real-image swap.

Rework `ServicesSection.tsx`'s existing card grid from its current thin-bordered text-only card into a photo-led card: each card gets an `aspect-[4/3]` (or similar) `<Image fill src={`/images/services/${service.slug}.jpg`} alt={service.name} className="object-cover">` image ABOVE the existing `name`/`summary`/`warranty.label` text (reserve the image's aspect ratio via a sized wrapper `div`, matching `TeamStrip.tsx`'s established pattern — this prevents the exact image-driven layout-shift problem `02-RESEARCH.md` Topic 1 flags for any new image without a reserved aspect ratio). Each card's image + name should link to `/services/${service.slug}` (wrap the image+name in a `next/link` `<Link>`; keep the summary/warranty-label as plain text below, or make the whole card a `<Link>` if that reads cleaner — Claude's discretion).

Add a single "View All Services" CTA (`Button as={Link} href="/services" variant="secondary"`, wrapped in `MagneticButton`, matching the pattern already used elsewhere in this codebase) below the grid, so the teaser has both per-card deep-links and one clear path to the full index.

Keep the existing constraint: this component still shows only `name`/`summary`/`warranty.label` — never `description` or `warranty.detail` (those stay exclusive to `/services/[slug]`, reinforced now by Task 1's real route existing to hold that depth).
  </action>
  <verify>`npx tsc --noEmit` passes. `grep -n "warranty.detail\|\.description" src/components/sections/ServicesSection.tsx` returns no matches (condensed fields only). Manually verify via dev server: the homepage Services section shows a 3-column (desktop) photo-led grid, each card's image+name links to the correct `/services/[slug]`, and a "View All Services" link/button leads to `/services`.</verify>
  <done>The homepage Services teaser is a 3-column photo-led grid linking to the real `/services` route and individual `/services/[slug]` pages, with all 7 images present and aspect-ratio-reserved (no layout shift on load).</done>
</task>

</tasks>

<verification>
`npm run build` succeeds with `/services` and `/services/[slug]` (7 params) present in the route list. `npx tsc --noEmit` passes. Manual check: homepage teaser and dedicated `/services` page both render correctly, and clicking through from the homepage teaser to any service's detail page shows the fuller `description`/`warranty.detail` content not present on the homepage.
</verification>

<success_criteria>
`/services` is reachable again with full per-service depth, no longer redirecting to the homepage anchor; the homepage's Services section reads as a photo-led teaser (matching Storey's 3-column presentation) rather than a minimal text-only card grid, and correctly funnels visitors to the full route for depth.
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-pages-home-services-and-about/02-14-SUMMARY.md`
</output>
