---
phase: 02-core-pages-home-services-and-about
plan: 12
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/sections/ContactModal.tsx
  - src/app/(marketing)/contact/page.tsx
autonomous: true

must_haves:
  truths:
    - "The sliding contact modal shows the full brief-intake field set (name, email, project type, location, timeline, message), not the current placeholder copy-only content"
    - "The standalone /contact fallback page (hard navigation) shows the same field set, not just contact details, so soft-nav and hard-nav visitors see equivalent content"
    - "No submit/persist/email logic is wired — the form does not call a Server Action or fetch endpoint (Phase 4 scope)"
  artifacts:
    - path: "src/components/sections/ContactModal.tsx"
      provides: "Full brief-intake form fields: name, email, project type, location (office), timeline, message"
      contains: "name=\"message\""
    - path: "src/app/(marketing)/contact/page.tsx"
      provides: "Same field set as ContactModal, rendered in-flow for hard navigation"
      contains: "name=\"message\""
  key_links:
    - from: "src/components/sections/ContactModal.tsx"
      to: "src/content/services.ts"
      via: "project type <select> options sourced from SERVICES names"
      pattern: "SERVICES"
---

<objective>
Expand the sliding contact modal's placeholder content into the full "spatial brief intake"-style form field set (name, email, project type, location, timeline, message) per `02-CONTEXT.md`'s decision to pull this field-richness forward from Phase 4 — still with zero submission/persistence/email logic, since that remains explicit Phase 4 scope (CONT-01/CONT-02).

Purpose: Phase 4's job becomes "wire up this existing form" instead of "design and build the form" — this plan delivers the field-complete, non-functional UI so Phase 4 can focus purely on the Server Action/email-delivery/error-handling layer.
Output: `ContactModal.tsx` (soft-nav modal) and `contact/page.tsx` (hard-nav fallback) both render the same field set, keeping content parity between the two entry paths per the existing 02-09 convention (they already share the same heading/copy structure).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-core-pages-home-services-and-about/02-CONTEXT.md
@.planning/phases/02-core-pages-home-services-and-about/02-RESEARCH.md
@src/components/sections/ContactModal.tsx
@src/app/(marketing)/contact/page.tsx
@src/content/services.ts
@src/components/ui/Button.tsx
@src/components/ui/Typography.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand ContactModal with the full brief-intake field set</name>
  <files>src/components/sections/ContactModal.tsx</files>
  <action>
Replace the current placeholder body (heading + copy + office/contact details + "Close" link) with a `<form>` (no `action`/`onSubmit` handler — a plain, non-functional `<form>` element; do NOT add `onSubmit={(e) => e.preventDefault()}` framing that implies working submission, just omit any submit wiring entirely) containing, in order, using existing design-system primitives (`Typography` for labels, native `<input>`/`<select>`/`<textarea>` styled with the project's semantic tokens — `border-line`, `bg-paper`, `text-ink`, `focus:` states — matching the visual weight of `Button`/`Typography` rather than unstyled browser defaults):

- **Name** — `<input type="text" name="name" required>`
- **Email** — `<input type="email" name="email" required>`
- **Project type** — `<select name="projectType">` with options sourced from `SERVICES.map((s) => s.name)` (import `SERVICES` from `@/content/services`) plus a leading "Select a project type" placeholder option — reuses real service names, no fabricated categories.
- **Location** — `<select name="location">` with options for the three real offices: "Solihull HQ", "London", "Nottingham" (matching the footer's existing office list — do not invent additional locations).
- **Timeline** — `<select name="timeline">` with a few honest, generic ranges (e.g. "Within 3 months", "3–6 months", "6–12 months", "Just exploring") — Claude's discretion on exact wording, no fabricated urgency claims.
- **Message** — `<textarea name="message" rows={4}>`
- A submit `<button type="button" disabled>` (deliberately `type="button"` and `disabled`, not `type="submit"`, so it's visually present but cannot be triggered/mistaken for working — add a short adjacent note or `title` attribute like "Submission coming soon" so it doesn't read as a silent bug) labeled "Send Enquiry" — OR, if a cleaner UX is preferred, keep it as a real `type="submit"` button with no `action`/`onSubmit` (native form submission with no handler does nothing but a full-page GET to the same URL with query params, which IS a real footgun) — **use `type="button" disabled` to avoid that footgun**, not `type="submit"`.

Preserve everything else about the existing component unchanged: the `role="dialog"` panel, backdrop-click/Escape dismiss, focus trap (the existing `FOCUSABLE_SELECTOR` query already picks up new `input`/`select`/`textarea`/non-disabled-button elements automatically — no changes needed there), the slide-up GSAP animation, and the office/contact details block (keep it below the form as a direct-contact alternative, per the existing content).

Label every field with a `<label htmlFor>` associated to a matching input `id` (accessibility — required regardless of Phase 4's future wiring).
  </action>
  <verify>`npx tsc --noEmit` passes. `grep -n 'name="message"' src/components/sections/ContactModal.tsx` and similar greps confirm all six fields are present. Manually verify via dev server: open the modal, Tab through every field in order, confirm the focus trap still wraps correctly at the new first/last focusable elements (now the name input and the disabled submit button, not the old "Close" link) — the previous "Close" text link may be removed if the disabled submit button provides sufficient dismissal context, but Escape/backdrop-click must remain the primary close paths regardless.</verify>
  <done>The contact modal renders all six brief-intake fields (name, email, project type, location, timeline, message) with real service names populating the project-type options, no working submit action, and the existing dismiss/focus-trap/animation behavior all still function correctly.</done>
</task>

<task type="auto">
  <name>Task 2: Mirror the same fields in the standalone /contact fallback page</name>
  <files>src/app/(marketing)/contact/page.tsx</files>
  <action>
Update `contact/page.tsx` (the hard-navigation fallback) to render the identical field set built in Task 1 — same labels, same `SERVICES`-sourced project-type options, same three office options, same timeline options, same disabled submit button — as an in-flow page section (using `TextReveal`/`Reveal` for the heading/intro, exactly as this file already does, but wrapping the new form fields in `Reveal` too rather than leaving them unanimated). Keep the office/contact details block below the form, matching `ContactModal`.

This keeps a hard-refresh visitor (someone who lands on `/contact` directly, e.g. from a shared link) seeing equivalent content to a soft-nav visitor who opened the modal — per the existing 02-09 precedent of keeping these two entry paths in content parity.
  </action>
  <verify>`npx tsc --noEmit` passes. `npm run build` succeeds. Manually diff the rendered field set between the modal (soft nav to `/contact`) and the standalone page (hard nav / direct browser load of `/contact`) — both show the same six fields with the same options.</verify>
  <done>`/contact`'s standalone fallback page shows the same brief-intake field set as the modal, keeping soft-nav and hard-nav content in parity.</done>
</task>

</tasks>

<verification>
`npm run build` and `npx tsc --noEmit` both pass. Neither file contains a working form submission path (no `action` attribute pointing at an endpoint, no `onSubmit` handler, no `fetch`/Server Action import) — grep confirms this stays purely presentational, consistent with Phase 4 owning the wiring.
</verification>

<success_criteria>
Both contact entry points (modal and standalone page) present the full brief-intake field set clients would expect from a real enquiry form, with zero functional submission — Phase 4 can wire a Server Action directly onto this existing markup without redesigning it.
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-pages-home-services-and-about/02-12-SUMMARY.md`
</output>
