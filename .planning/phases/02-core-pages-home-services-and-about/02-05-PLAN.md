---
phase: 02-core-pages-home-services-and-about
plan: 05
type: execute
wave: 3
depends_on: ["02-02", "02-03", "02-04"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "All Phase 2 pages (home, /work, all 7 services, about) render correctly across mobile, tablet, and desktop breakpoints"
    - "Headings across Phase 2 pages use text-reveal animation and primary CTA buttons show magnetic/hover micro-interactions, confirmed by the user viewing the live site"
  artifacts: []
  key_links: []
---

<objective>
Human-verify Phase 2's cross-cutting success criteria that can't be confirmed by static code inspection alone: responsive rendering across breakpoints (ENG-05) and the felt quality of the motion system (MOTION-01/MOTION-02) across every page built in this phase.

Purpose: Plans 02-01 through 02-04 each verify their own artifacts in isolation (build succeeds, SSR content present, primitives compile). This plan is the phase-level integration checkpoint — confirming the pages actually look and feel Awwwards-grade together, on real viewports, before Phase 2 is marked complete in STATE.md/ROADMAP.md.
Output: User confirmation that Phase 2's 5 ROADMAP.md success criteria hold on the live dev server; any issues found are logged as follow-up, not silently accepted.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated pre-checks before human verification</name>
  <files></files>
  <action>
Before handing off to the user, run and confirm automated checks pass so the human checkpoint is spent on genuine visual/UX judgment, not catching build errors:
- `npm run build` — must succeed with `/`, `/work`, `/services`, all 7 `/services/[slug]`, and `/about` listed as static routes.
- `npx tsc --noEmit` — must pass with zero errors across all Phase 2 files.
- Start the dev server (`npm run dev`) and leave it running for the human-verify step below.
  </action>
  <verify>Build output and typecheck both succeed with no errors.</verify>
  <done>All Phase 2 routes build statically and typecheck cleanly; dev server is running and ready for manual verification.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Full Phase 2: cinematic homepage hero (HOME-01) with persistent above-fold CTAs (HOME-02) and a credentials strip (TRUST-02), 7 dedicated service pages each with per-service warranty content (SERV-01/SERV-02), an About/team page with the 30+ years story and a photo-ready team grid (ABOUT-01), text-reveal headings (MOTION-01) and magnetic CTA buttons (MOTION-02) across all of the above, and a placeholder /work route.
  </what-built>
  <how-to-verify>
With the dev server running (`npm run dev`, default http://localhost:3000):

1. Open `/` — confirm the hero headline reveals word-by-word on load/scroll, supporting copy and CTA row reveal in sequence shortly after, "Enquire" and "View Projects" are both visible without scrolling, and hovering either CTA with a mouse shows a magnetic cursor-follow effect. Scroll down to confirm the credentials strip (JCT/10yr/3yr) is visible and legible.
2. Open `/services` — confirm all 7 services are listed and linked. Open at least 3 of the 7 individual service pages — confirm each shows real, distinct warranty/trust content (not identical placeholder text).
3. Open `/about` — confirm the 30+ years story renders and the team grid displays (photo tiles or neutral initials placeholders — confirm no stock photography appears anywhere).
4. Open `/work` — confirm it's a real page (not a 404) with placeholder messaging.
5. Using browser devtools device toolbar (or resizing the window), check `/`, one service page, and `/about` at a mobile width (~375px), tablet width (~768px), and desktop width (~1440px) — confirm no layout breakage, overlapping text, or unusable CTAs at any size.
6. Toggle OS/browser "reduce motion" (or emulate `prefers-reduced-motion: reduce` in devtools Rendering tab) and reload `/` — confirm headline/copy/CTAs appear instantly with no animation, and magnetic hover no longer follows the cursor.

Report back either "approved" or a list of specific issues (page + breakpoint + what's wrong).
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to address</resume-signal>
</task>

</tasks>

<verification>
All 5 ROADMAP.md Phase 2 success criteria confirmed true by the user against the live dev server, not just inferred from passing builds.
</verification>

<success_criteria>
User has explicitly approved Phase 2 across responsive breakpoints and motion quality; any issues raised are captured for a gap-closure plan rather than left unaddressed.
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-pages-home-services-and-about/02-05-SUMMARY.md`
</output>
