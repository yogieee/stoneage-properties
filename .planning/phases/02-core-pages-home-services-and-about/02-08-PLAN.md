---
phase: 02-core-pages-home-services-and-about
plan: 08
type: execute
wave: 2
depends_on: ["02-06", "02-07"]
files_modified:
  - src/app/(marketing)/page.tsx
  - src/app/(marketing)/layout.tsx
  - src/components/sections/SiteNav.tsx
  - src/components/motion/SmoothScrollProvider.tsx
  - src/components/sections/ContactCtaStrip.tsx
autonomous: true

must_haves:
  truths:
    - "Visitor scrolling the homepage sees Hero, then Services, then Work preview, then About, then a Contact CTA strip, in that order, as sections of one page"
    - "Nav shows only a logo (home link), Projects, Contact, plus in-page anchor links to Services/Work/About that smooth-scroll within /"
    - "The nav link matching the section currently in view is visually highlighted while scrolling"
  artifacts:
    - path: "src/app/(marketing)/page.tsx"
      provides: "Single scrolling page: Hero + Services + Work preview + About + Contact CTA sections, each with a stable id"
      contains: "id=\"services\""
    - path: "src/components/sections/SiteNav.tsx"
      provides: "Client nav component: logo/home link, anchor links, Projects, Contact, scroll-spy active-link highlighting"
      min_lines: 30
    - path: "src/app/(marketing)/layout.tsx"
      provides: "Marketing layout composing SiteNav instead of the old flat NAV_LINKS list"
      contains: "SiteNav"
  key_links:
    - from: "src/components/sections/SiteNav.tsx"
      to: "src/app/(marketing)/page.tsx"
      via: "ScrollTrigger.create({ trigger: \"#services\", toggleClass })"
      pattern: "ScrollTrigger.create"
    - from: "src/components/motion/SmoothScrollProvider.tsx"
      to: "Lenis anchors option"
      via: "options={{ autoRaf: false, anchors: true }}"
      pattern: "anchors"
---

<objective>
Compose the single scrolling homepage (`/`) from 02-07's condensed sections plus the existing Hero, and rebuild the global nav into the simplified logo + anchor-links + Projects + Contact structure with scroll-spy active-link highlighting, per `02-CONTEXT.md`'s Storey Architecture reference.

Purpose: This is the structural heart of the pivot — turning four previously separate routes' worth of content into one coherent scroll narrative, and turning the old five-item flat nav into the minimal single-page nav the user explicitly asked for. Everything needed to build this (sections, routes) already exists after Wave 1 (02-06, 02-07).
Output: `/` renders Hero → Services → Work preview → About → Contact CTA as one page; a new `SiteNav` component drives smooth anchor-scrolling (Lenis `anchors: true`) and scroll-spy active-link highlighting (`ScrollTrigger` + `toggleClass`, per `02-RESEARCH.md` Topic 1) without a hand-rolled `IntersectionObserver`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-core-pages-home-services-and-about/02-CONTEXT.md
@.planning/phases/02-core-pages-home-services-and-about/02-RESEARCH.md
@src/app/(marketing)/layout.tsx
@src/components/sections/Hero.tsx
@src/components/motion/SmoothScrollProvider.tsx
@src/lib/gsap.ts
@src/components/ui/Button.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Compose the single page with a Contact CTA strip</name>
  <files>src/app/(marketing)/page.tsx, src/components/sections/ContactCtaStrip.tsx</files>
  <action>
Create `src/components/sections/ContactCtaStrip.tsx`: a server component with a `TextReveal` heading (e.g. "Let's build something") and a `Reveal`-wrapped `Button as={Link} href="/contact" variant="primary"` "Enquire" CTA wrapped in `MagneticButton` — this is a CTA strip, not a form (the actual contact modal/form is built in Wave 3, 02-09; this strip is just the trigger surface, matching `02-CONTEXT.md`'s "Contact CTA strip (triggers the contact modal — not a full section/route)").

Rewrite `src/app/(marketing)/page.tsx` to compose, in order:
1. `<Hero />` (unchanged, already has its own internal layout — no wrapping `<section id>` needed since it's not a nav anchor target)
2. `<section id="services"><ServicesSection /></section>`
3. `<section id="work"><WorkPreviewSection /></section>`
4. `<section id="about"><AboutSection /></section>`
5. `<ContactCtaStrip />` (no `id` needed — not a scroll-spy anchor target per `02-CONTEXT.md`, it's a CTA strip not a browsable section)

Each `<section>` wrapping a condensed component should have consistent vertical padding/spacing (match the existing `px-6 py-24 sm:px-12` convention from `Hero.tsx`/`work/page.tsx`) so the page reads as one coherent scroll, not visually disjointed blocks.
  </action>
  <verify>`npm run build` succeeds; `curl -s http://localhost:3000/ | grep -o 'id="[a-z]*"'` (dev server) shows `id="services"`, `id="work"`, `id="about"` present in SSR HTML.</verify>
  <done>`/` renders Hero, Services, Work preview, About, and a Contact CTA strip in order, each section-level id present in server-rendered HTML (confirms ENG-04 server-rendering discipline holds for the new composition).</done>
</task>

<task type="auto">
  <name>Task 2: SiteNav — simplified links, smooth anchor scroll, scroll-spy highlighting</name>
  <files>src/components/sections/SiteNav.tsx, src/app/(marketing)/layout.tsx, src/components/motion/SmoothScrollProvider.tsx</files>
  <action>
Update `src/components/motion/SmoothScrollProvider.tsx`: add `anchors: true` to the existing `options={{ autoRaf: false }}` object passed to `ReactLenis root` (per `02-RESEARCH.md` Topic 1 — this is a one-line addition, declarative, makes every `<a href="#...">` in the app smooth-scroll via Lenis with zero new click-handler code).

Create `src/components/sections/SiteNav.tsx` (`"use client"`, since it needs `useGSAP` for scroll-spy):
- Renders: logo/wordmark as `<Link href="/">Stoneage Properties</Link>` (acts as the home link, replacing the old separate "Home" nav item), then anchor links `Services` (`href="/#services"`), `Work` (`href="/#work"`), `About` (`href="/#about"`) — using the `/#id` form (not bare `#id`) so these links resolve correctly from any route, not just `/` — then `Projects` (`href="/projects"`, a real route link) and `Contact` (`href="/contact"`, will be intercepted by the modal once 02-09 lands; a plain `next/link` Link needs no special-casing per `02-RESEARCH.md` Topic 2).
- Scroll-spy: inside a `useGSAP()` call (no `scope` ref needed since triggers reference sections by string selector, not local refs — per `02-RESEARCH.md` Pitfall 4, string selectors like `"#services"` resolve lazily and avoid the "nav mounts before sections" timing issue), create one `ScrollTrigger.create({ trigger: "#services", start: "top center", end: "bottom center", toggleClass: { targets: 'a[href="/#services"]', className: "is-active" } })` per section id (`services`, `work`, `about`). Do NOT use `IntersectionObserver` (`02-RESEARCH.md` "Don't Hand-Roll" table — a second independent scroll-position system would drift from Lenis's smoothed position).
- Add a minimal `.is-active` visual treatment (e.g. `text-ink` vs a muted default, or an underline) via a scoped `<style jsx>` or a Tailwind-compatible approach consistent with the project's semantic-token-only convention (no raw gray/slate/zinc utilities) — e.g. default anchor links use `text-ink-muted`, `.is-active` overrides to `text-ink font-medium`.
- Scroll-spy highlighting runs unconditionally regardless of `prefers-reduced-motion` (it's a state toggle, not a motion effect, per `02-RESEARCH.md` Topic 1's explicit note) — only the smooth-scroll-to-section motion (handled entirely by Lenis's `anchors` option, not custom code here) is subject to Lenis's own reduced-motion default.

Update `src/app/(marketing)/layout.tsx`: replace the current flat `NAV_LINKS` array + inline `<nav>` markup with `<SiteNav />`, keeping the same `<header>` wrapper/border styling. Remove the now-unused `NAV_LINKS` constant.
  </action>
  <verify>`npx tsc --noEmit` passes. Manually verify (dev server): clicking "Services" in the nav smooth-scrolls to the Services section; scrolling the page causes the corresponding nav link to gain a visible active-state style as each section crosses the viewport center; resizing to mobile width keeps the nav usable (no overflow).</verify>
  <done>Global nav shows exactly logo/home + Services/Work/About anchors + Projects + Contact; clicking an anchor smooth-scrolls via Lenis; the nav link for the currently-centered section is visually distinguished from the others while scrolling, driven by `ScrollTrigger`/`toggleClass` (not `IntersectionObserver`).</done>
</task>

</tasks>

<verification>
`npm run build` succeeds with `/` as the only marketing content route besides `/projects` and `/style-guide`. `npx tsc --noEmit` passes. Manual scroll-spy check confirms `ScrollTrigger` (not a competing `IntersectionObserver`) drives nav highlighting, staying in sync with Lenis per the already-established `LenisGsapSync` wiring.
</verification>

<success_criteria>
The homepage is the single coherent scroll experience `02-CONTEXT.md` describes, with a minimal nav (logo + Projects + Contact + in-page anchors) that smooth-scrolls and highlights the active section — matching the Storey Architecture reference tone without any hand-rolled scroll-position code competing with the existing Lenis/GSAP sync.
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-pages-home-services-and-about/02-08-SUMMARY.md`
</output>
