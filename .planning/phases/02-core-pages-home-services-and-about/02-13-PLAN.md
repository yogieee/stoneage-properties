---
phase: 02-core-pages-home-services-and-about
plan: 13
type: execute
wave: 2
depends_on: ["02-11"]
files_modified:
  - src/components/sections/HeroCarousel.tsx
  - src/components/sections/Hero.tsx
  - public/images/hero/hero-1.jpg
  - public/images/hero/hero-2.jpg
  - public/images/hero/hero-3.jpg

autonomous: true

must_haves:
  truths:
    - "The homepage hero shows a full-viewport, multi-image carousel crossfading behind the headline, replacing the current plain-background hero"
    - "The first hero image renders at full opacity immediately (priority-loaded, not GSAP-faded-in) so it does not regress LCP"
    - "Under prefers-reduced-motion: reduce, the carousel shows a single static slide and does not auto-cycle; a visible pause/play control exists regardless of the reduced-motion setting (WCAG 2.2.2)"
  artifacts:
    - path: "src/components/sections/HeroCarousel.tsx"
      provides: "Full-viewport GSAP crossfade carousel, N absolutely-positioned Image layers"
      min_lines: 40
    - path: "public/images/hero/hero-1.jpg"
      provides: "Self-hosted stock hero image 1 (priority/LCP slide)"
  key_links:
    - from: "src/components/sections/Hero.tsx"
      to: "src/components/sections/HeroCarousel.tsx"
      via: "component composition, HeroCarousel rendered behind the headline content"
      pattern: "HeroCarousel"
    - from: "src/components/sections/HeroCarousel.tsx"
      to: "next/image"
      via: "priority prop on the first slide only"
      pattern: "priority"
---

<objective>
Replace the homepage Hero's current plain-background treatment with a full-viewport, multi-image GSAP crossfade carousel behind the headline, matching storeyarchitecture.co.uk's reference behavior, using self-hosted stock photography as a deliberate, temporary, user-approved exception to REQUIREMENTS.md's stock-photo exclusion (per `02-CONTEXT.md`'s explicit sign-off — flagged here again for visibility, not silently exercised).

Purpose: The hero carousel is the single most visible piece of the Storey-replication design direction the user asked for at the 02-10 checkpoint. Building it on top of 02-11's already-fixed `ScrollTrigger`/`SplitText` primitives avoids compounding a new animated component on top of the just-fixed bug class.
Output: `HeroCarousel.tsx` (new, `components/sections/` — domain-aware since it takes image data, not `components/motion/`), `Hero.tsx` updated to render it behind the existing headline/CTA content, and 3 self-hosted stock images in `public/images/hero/`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-core-pages-home-services-and-about/02-CONTEXT.md
@.planning/phases/02-core-pages-home-services-and-about/02-RESEARCH.md
@src/components/sections/Hero.tsx
@src/components/motion/Reveal.tsx
@src/components/motion/TextReveal.tsx
@src/lib/gsap.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Source stock photography and build HeroCarousel</name>
  <files>src/components/sections/HeroCarousel.tsx, public/images/hero/hero-1.jpg, public/images/hero/hero-2.jpg, public/images/hero/hero-3.jpg</files>
  <action>
**Source 3 self-hosted stock images first** (per `02-RESEARCH.md` Topic 6's recommendation: self-host in `public/`, do not hotlink, to avoid `next.config.ts` `images.remotePatterns` config and get automatic width/height inference which also helps prevent layout-shift): download 3 free-to-use (Unsplash License or equivalent) UK residential architecture/construction-style photographs — search terms like "modern house exterior", "residential construction site", "architect building exterior UK" — and save them as `public/images/hero/hero-1.jpg`, `hero-2.jpg`, `hero-3.jpg` (reasonable web-optimized size, ~1920px wide). If network access to an image source is unavailable in this execution environment, generate 3 placeholder JPGs of a plausible hero aspect ratio (e.g. via ImageMagick `convert -size 1920x1080 gradient:'#2b2b2b'-'#050505' hero-N.jpg` or an equivalent available tool) as an explicitly temporary stand-in, and flag this clearly in the SUMMARY as needing a real-image swap before this is considered visually complete — do not silently ship blank/broken image files.

Create `src/components/sections/HeroCarousel.tsx` (`"use client"`, domain-aware — takes a `slides: { src: string; alt: string }[]` prop, defaulting to the 3 sourced images if no prop given, so `Hero.tsx` can compose it with zero boilerplate):
- Render N absolutely-positioned full-bleed `<Image fill className="object-cover">` layers stacked in the same container (`position: relative` on the wrapper, each `Image`'s wrapper `position: absolute inset-0`), one visible (`opacity/autoAlpha: 1`) at a time, the rest hidden (`autoAlpha: 0`).
- The FIRST slide gets `priority` on its `next/image` (skip lazy-loading — it is very likely this page's LCP element) and must render at full opacity on mount with NO GSAP fade-in — only slides 2/3+ (the crossfade cycling) are GSAP-animated. Do not wrap the first slide's initial appearance in any `opacity: 0` starting state.
- Crossfade mechanics: inside a `useGSAP()` call, build a `gsap.timeline({ repeat: -1 })` that holds each slide for a few seconds then crossfades `autoAlpha` (not `display`/`visibility`, not `width`/`height`/`top`/`left` — compositor-friendly properties only) to the next slide in sequence, looping back to slide 1 after the last.
- **Reduced-motion is a SECOND, INDEPENDENT `gsap.matchMedia()` check from `Reveal`/`TextReveal`'s contract** (per `02-RESEARCH.md` Topic 2/Pitfall 3): under `prefers-reduced-motion: reduce`, do NOT build the looping timeline at all — show only the first slide, statically, at full opacity, with no auto-cycling (not just an instant/undamped version of the same loop — the carousel must not move on its own at all under reduced motion).
- **WCAG 2.2.2 pause control**: render a small, keyboard-reachable pause/play toggle button (icon or text, e.g. "Pause"/"Play") positioned in a corner of the hero (e.g. `absolute bottom-6 right-6 z-10`), wired to `timeline.pause()`/`timeline.play()`. This control must exist and function REGARDLESS of the `prefers-reduced-motion` setting (per WCAG's own Level A requirement, independent of the OS media-query preference) — even though under reduced motion the timeline never starts, the control should still be present (it can no-op or stay in a "paused" visual state in that case, but must not be removed from the DOM).
- Add a subtle dark gradient/overlay (`bg-gradient-to-t from-ink/50 to-transparent` or similar, using existing semantic tokens) between the image layers and the headline content so text contrast is preserved regardless of which stock image is showing — this is a visual necessity once real photography sits behind white/dark headline text, not present in the current plain-background Hero.
  </action>
  <verify>`npx tsc --noEmit` passes. Manually verify via dev server: the hero shows a crossfading background image cycling every few seconds; the first image is visible immediately on load (check Network tab — first image request has no lazy-loading `loading="lazy"` attribute, confirming `priority`); toggling `prefers-reduced-motion: reduce` in devtools and reloading shows a single static first slide with the pause control still present but the carousel not cycling; the pause/play control visibly stops/resumes cycling under normal motion.</verify>
  <done>`HeroCarousel` crossfades 3 self-hosted images behind the hero content, respects reduced-motion by showing a static first slide with no auto-cycling, provides a working pause/play control independent of the reduced-motion setting, and does not regress LCP (first slide renders immediately, `priority`-loaded, no fade-in).</done>
</task>

<task type="auto">
  <name>Task 2: Integrate HeroCarousel into Hero.tsx</name>
  <files>src/components/sections/Hero.tsx</files>
  <action>
Update `Hero.tsx` to render `<HeroCarousel />` as a full-bleed background layer (`absolute inset-0 -z-10` or equivalent, sized to fill the existing `min-h-[85vh]` hero section) behind the existing `TextReveal`/`Reveal`/`MagneticButton` headline/CTA content, which stays otherwise unchanged in structure. Adjust the headline/copy text color if needed for contrast against photography (e.g. switching `text-ink` to a lighter token, or relying on the gradient overlay from Task 1 — Claude's discretion on exact treatment, but text must remain clearly legible against all 3 rotating background images).

Remove or reposition the existing `BlueprintAccent` decorative SVG if it visually conflicts with the new photographic background (a thin-line blueprint motif may read as visual noise over a photo-led hero, which is the exact "wireframe/diagram-led" critique from the 02-10 rejection this pivot is meant to resolve) — Claude's discretion on removing it entirely from the Hero vs. relocating it elsewhere, but do not leave it layered confusingly over the new carousel without a deliberate visual reason.
  </action>
  <verify>`npx tsc --noEmit` passes. `npm run build` succeeds. Manually verify via dev server: `/` shows the hero headline and CTAs clearly legible over the crossfading photo background at mobile (~375px), tablet (~768px), and desktop (~1440px) widths, with no layout shift from the image layers loading in.</verify>
  <done>The homepage hero is now a full-viewport photo-led carousel with legible headline/CTA content layered on top, replacing the prior plain-background/blueprint-accent treatment, matching the Storey reference direction.</done>
</task>

</tasks>

<verification>
`npm run build` and `npx tsc --noEmit` both pass. Manual verification confirms the carousel crossfades, respects reduced-motion (static first slide, no auto-cycle), has a working pause control, and does not visually break the headline/CTA layer at any breakpoint.
</verification>

<success_criteria>
The homepage hero matches the Storey reference's full-viewport multi-image carousel behavior, is accessible (WCAG 2.2.2 pause control, correct reduced-motion fallback), and does not regress LCP or introduce layout shift — built on top of 02-11's already-fixed animation primitives.
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-pages-home-services-and-about/02-13-SUMMARY.md`
</output>
