---
phase: 02-core-pages-home-services-and-about
plan: 16
type: execute
wave: 2
depends_on: ["02-11"]
files_modified:
  - src/components/decorative/SectionDivider.tsx
  - src/components/sections/Testimonials.tsx
  - src/content/testimonials.ts
  - src/app/(marketing)/page.tsx

autonomous: true

must_haves:
  truths:
    - "The homepage shows a decorative divider element between at least the Services/Work/About/Testimonials sections, giving visual breathing room between sections in the spirit of Storey's paper/clip motif, adapted to Stoneage's monochrome/editorial tokens"
    - "The homepage includes a Testimonials carousel section (a few client quotes cycling), positioned before the closing Contact CTA strip"
    - "The testimonials carousel respects prefers-reduced-motion (static single quote, no auto-cycling) exactly like the hero carousel's contract"
  artifacts:
    - path: "src/components/decorative/SectionDivider.tsx"
      provides: "Reusable monochrome decorative divider (SVG or clip-path), sibling to BlueprintAccent"
      min_lines: 15
    - path: "src/components/sections/Testimonials.tsx"
      provides: "GSAP-only testimonials carousel, reduced-motion aware"
      min_lines: 30
    - path: "src/content/testimonials.ts"
      provides: "Typed array of a few client quotes"
      contains: "quote"
  key_links:
    - from: "src/app/(marketing)/page.tsx"
      to: "src/components/sections/Testimonials.tsx"
      via: "component composition, placed before ContactCtaStrip"
      pattern: "Testimonials"
    - from: "src/components/sections/Testimonials.tsx"
      to: "src/content/testimonials.ts"
      via: "import { TESTIMONIALS } from \"@/content/testimonials\""
      pattern: "TESTIMONIALS"
---

<objective>
Add the two remaining new homepage elements from the Storey-replication direction — decorative section dividers and a testimonials carousel — and compose them into the single-page homepage, per `02-CONTEXT.md`'s decisions. This plan owns all edits to `app/(marketing)/page.tsx` for this wave, to avoid file-conflict with the other wave-2 plans that touch section-internal files only.

Purpose: Dividers give the page the "breathing room between sections" quality the Storey reference has; testimonials add a trust-signal section explicitly called for in `02-CONTEXT.md` as new (not deferred) scope. Both are GSAP-only/zero-new-dependency per `02-RESEARCH.md` Topics 3 and 7, extending existing in-codebase patterns (`BlueprintAccent`-style decorative SVGs, the hero carousel's crossfade approach from `02-13`).
Output: `SectionDivider.tsx` (new decorative component), `Testimonials.tsx` + `content/testimonials.ts` (new section + content), and `page.tsx` updated to interleave dividers between sections and insert the Testimonials section before the closing `ContactCtaStrip`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-core-pages-home-services-and-about/02-CONTEXT.md
@.planning/phases/02-core-pages-home-services-and-about/02-RESEARCH.md
@src/app/(marketing)/page.tsx
@src/components/decorative/BlueprintAccent.tsx
@src/components/motion/Reveal.tsx
@src/lib/gsap.ts
@src/content/team.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: SectionDivider decorative component</name>
  <files>src/components/decorative/SectionDivider.tsx</files>
  <action>
Create `src/components/decorative/SectionDivider.tsx`, a sibling to `BlueprintAccent.tsx` following the exact same content-agnostic, no-domain-props contract (`stroke="currentColor"`/`fill="currentColor"` only inheriting semantic `text-*` tokens, `aria-hidden="true"`, no text, positioning/sizing left to the consumer via `className`). Implement a torn-edge/zigzag or angled-wedge SVG path (or, if a full-bleed-width effect is wanted without SVG viewBox scaling concerns, a CSS `clip-path: polygon(...)` variant applied via `className` to a plain `<div>` — pick ONE approach and use it consistently across all divider usages, per `02-RESEARCH.md` Topic 7's recommendation to keep the decorative-layer technique singular) that reads as a lightweight, monochrome-adapted echo of Storey's paper/clip motif — Claude's discretion on exact shape (torn-edge, geometric wedge, thin ruled-line-with-tick).
  </action>
  <verify>`npx tsc --noEmit` passes. Manually verify the component renders visibly (a thin decorative shape, not invisible/zero-size) when dropped between two sections with `bg-paper`/`bg-paper-dim` contrast.</verify>
  <done>`SectionDivider` exists as a reusable, monochrome, content-agnostic decorative component ready to be placed between homepage sections.</done>
</task>

<task type="auto">
  <name>Task 2: Testimonials carousel</name>
  <files>src/components/sections/Testimonials.tsx, src/content/testimonials.ts</files>
  <action>
Create `src/content/testimonials.ts` exporting a typed `TESTIMONIALS` array (`{ quote: string; author: string; role?: string }[]`, 3-5 entries) with realistic, honest client-quote-style copy consistent with Stoneage's real service offering (new builds/renovations/extensions/conversions, value for money, quick communication, professional expertise — the same brand-story language already used in `AboutSection`) — do NOT fabricate named real clients or specific project claims; use generic-but-plausible attribution (e.g. "New Build client, Solihull" rather than a fabricated full name+photo implying a real identifiable person), consistent with the same honesty precedent `content/team.ts` and `WorkPreviewSection.tsx` already established for placeholder content.

Create `src/components/sections/Testimonials.tsx` (`"use client"`, GSAP-only per `02-RESEARCH.md` Topic 3's recommendation — no Embla, reuses the same crossfade-timeline pattern `02-13`'s `HeroCarousel` establishes, just simpler since there's no LCP concern below the fold):
- `TextReveal as="h2"` section heading (e.g. "What Clients Say").
- One quote card visible at a time (`Typography variant="body-lg"` for the quote, `Typography variant="body-sm"` for the author/role), cycling via a `gsap.timeline({ repeat: -1 })` crossfading `autoAlpha` between quote cards, same compositor-friendly-properties-only discipline as the hero carousel.
- Independent `gsap.matchMedia()` reduced-motion branch (same contract as `HeroCarousel`): under reduced motion, show a single static quote, no auto-cycling.
- Optional simple prev/next arrow controls (Claude's discretion per `02-CONTEXT.md` — "autoplay, arrows/dots, swipe" left open); at minimum, no swipe/drag is required (GSAP `Draggable` only if it feels warranted, not required).
  </action>
  <verify>`npx tsc --noEmit` passes. Manually verify via dev server: the Testimonials section cycles between quotes automatically; toggling `prefers-reduced-motion: reduce` shows a single static quote with no cycling.</verify>
  <done>`Testimonials` renders a working, reduced-motion-aware quote carousel sourced from `content/testimonials.ts`, using only honest generic client attribution.</done>
</task>

<task type="auto">
  <name>Task 3: Compose dividers + Testimonials into the homepage</name>
  <files>src/app/(marketing)/page.tsx</files>
  <action>
Update `page.tsx` to:
- Insert a `<SectionDivider className="..." />` between each major section boundary (at minimum: Hero→Services, Services→Work, Work→About, About→Testimonials) — sized/positioned via `className`, using the single technique chosen in Task 1 consistently at every insertion point.
- Insert `<Testimonials />` (no `id` needed unless it should also be a scroll-spy anchor target — Claude's discretion; if added as an anchor, update `SiteNav.tsx`'s `ANCHOR_LINKS` accordingly in a way that doesn't conflict with `02-15`'s nav edits, e.g. by leaving `SiteNav.tsx` untouched in this plan and only wiring the anchor if it's low-risk — default to NOT adding an id/anchor for Testimonials unless it's trivial, keeping this plan's file scope limited to `page.tsx` as declared in frontmatter) between the `about` section and `ContactCtaStrip`.
  </action>
  <verify>`npm run build` succeeds. Manually verify via dev server: scrolling `/` from top to bottom shows Hero → divider → Services → divider → Work → divider → About → divider → Testimonials → Contact CTA strip, with dividers visible at each transition and Testimonials cycling quotes before the final CTA.</verify>
  <done>The homepage composition includes decorative dividers between every major section and a working Testimonials carousel before the closing Contact CTA strip.</done>
</task>

</tasks>

<verification>
`npm run build` and `npx tsc --noEmit` both pass. Manual scroll-through of `/` confirms dividers appear at each section boundary and Testimonials cycles correctly, respecting reduced-motion.
</verification>

<success_criteria>
The homepage gains the Storey-reference "breathing room between sections" quality via dividers and a working testimonials trust-signal section, composed without touching any file already owned by another wave-2 plan (`02-13`, `02-14`, `02-17`).
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-pages-home-services-and-about/02-16-SUMMARY.md`
</output>
