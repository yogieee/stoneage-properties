---
phase: 02-core-pages-home-services-and-about
plan: 17
type: execute
wave: 2
depends_on: ["02-11"]
files_modified:
  - src/components/sections/WorkPreviewSection.tsx
  - src/components/sections/AboutSection.tsx
  - public/images/work/work-preview.jpg
  - public/images/about/about-office.jpg

autonomous: true

must_haves:
  truths:
    - "The Work preview section shows a representative stock photo alongside its existing honest 'archive being finalized' copy, matching the sitewide photo-led placeholder strategy"
    - "The About section shows a representative stock photo (office/team ambiance) alongside its existing brand-story copy, distinct from and in addition to the TeamStrip's individual member tiles"
    - "TeamStrip's per-member photo/initials-tile contract is unchanged — the new About section photo is a separate, general-purpose image, never a substitute for a real team member photo"
  artifacts:
    - path: "public/images/work/work-preview.jpg"
      provides: "Self-hosted stock image for the Work preview teaser"
    - path: "public/images/about/about-office.jpg"
      provides: "Self-hosted stock image for the About section"
  key_links:
    - from: "src/components/sections/WorkPreviewSection.tsx"
      to: "public/images/work/work-preview.jpg"
      via: "next/image with a reserved aspect-ratio wrapper"
      pattern: "images/work"
    - from: "src/components/sections/AboutSection.tsx"
      to: "public/images/about/about-office.jpg"
      via: "next/image with a reserved aspect-ratio wrapper"
      pattern: "images/about"
---

<objective>
Extend the sitewide stock-photography placeholder strategy (already applied to Hero in `02-13` and Services in `02-14`) to the Work preview and About sections, per `02-CONTEXT.md`'s explicit decision that this is a "consistent placeholder strategy sitewide, not mixed per-section." This is a deliberate, temporary, user-approved exception to REQUIREMENTS.md's stock-photo exclusion — flagged here again for visibility.

Purpose: Without this plan, Work preview and About would remain text-only while Hero/Services become photo-led, breaking the "consistent... sitewide" requirement and leaving the page visually inconsistent — some sections photo-led, others not, undermining the Storey-replication goal.
Output: `WorkPreviewSection.tsx` and `AboutSection.tsx` each gain one representative, aspect-ratio-reserved stock image; `TeamStrip.tsx`'s existing per-member photo-or-initials contract (ABOUT-01's "never a stock photo as fallback for a real person" constraint) is left completely untouched — the new About image is general/ambient, not a substitute for any team member's photo.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-core-pages-home-services-and-about/02-CONTEXT.md
@.planning/phases/02-core-pages-home-services-and-about/02-RESEARCH.md
@src/components/sections/WorkPreviewSection.tsx
@src/components/sections/AboutSection.tsx
@src/components/sections/TeamStrip.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Photo for WorkPreviewSection</name>
  <files>src/components/sections/WorkPreviewSection.tsx, public/images/work/work-preview.jpg</files>
  <action>
Source one self-hosted stock image (same sourcing approach as `02-13`/`02-14`: free/Unsplash-License, UK residential-construction-style, ~1200-1600px wide; if network access is unavailable, generate a placeholder and flag it in the SUMMARY) representing a completed/in-progress construction project generally (not a specific fabricated project — this section's copy already honestly states no real project data exists yet; the image is ambient/representative, not a claim about a specific project), saved as `public/images/work/work-preview.jpg`.

Update `WorkPreviewSection.tsx` to add this image (aspect-ratio-reserved via a sized wrapper `div` + `next/image fill`, matching `TeamStrip.tsx`'s established pattern to avoid layout shift) alongside the existing heading/copy/CTA — Claude's discretion on exact layout (image beside the text on desktop, stacked on mobile, or a full-width image above the text). Do not remove or alter the existing honest "archive being finalized" copy — the image supplements it, it does not imply real curated project photos exist yet.
  </action>
  <verify>`npx tsc --noEmit` passes. Manually verify via dev server: the Work preview section shows the image with no layout shift on load (aspect ratio reserved), and the existing honest copy/CTA are unchanged.</verify>
  <done>`WorkPreviewSection` shows one representative, aspect-ratio-reserved stock image alongside its unchanged honest copy and `/projects` CTA.</done>
</task>

<task type="auto">
  <name>Task 2: Photo for AboutSection</name>
  <files>src/components/sections/AboutSection.tsx, public/images/about/about-office.jpg</files>
  <action>
Source one self-hosted stock image representing a general office/team-at-work ambiance (not a specific person — this must not read as or substitute for any individual team member's photo, preserving ABOUT-01's "never a stock photo as fallback for a real person" constraint, which governs `TeamStrip` only and is untouched by this plan), saved as `public/images/about/about-office.jpg`.

Update `AboutSection.tsx` to add this image (aspect-ratio-reserved, same pattern as Task 1) near the top of the section, above or beside the brand-story paragraphs, clearly visually distinct from the `TeamStrip` grid below it (which keeps its existing per-member photo-or-initials tiles unchanged) — the two image treatments should not be visually confusable as the same kind of content.
  </action>
  <verify>`npx tsc --noEmit` passes. `grep -n "unsplash\|placeholder" src/components/sections/TeamStrip.tsx` still returns no matches (TeamStrip itself untouched). Manually verify via dev server: the About section shows the new ambient image plus the existing brand-story copy plus the unchanged `TeamStrip` grid, with no layout shift on load.</verify>
  <done>`AboutSection` shows one representative, aspect-ratio-reserved ambient stock image, clearly distinct from `TeamStrip`'s unchanged per-member photo-or-initials tiles, with the existing brand-story copy unchanged.</done>
</task>

</tasks>

<verification>
`npx tsc --noEmit` passes. `npm run build` succeeds. `TeamStrip.tsx` is unmodified (not in this plan's `files_modified`) — its per-member neutral-placeholder contract is verifiably untouched.
</verification>

<success_criteria>
Work preview and About now match Hero/Services' photo-led treatment, completing the "consistent sitewide" stock-photography placeholder strategy `02-CONTEXT.md` calls for, without compromising ABOUT-01's constraint that real team members never get a stock-photo substitute.
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-pages-home-services-and-about/02-17-SUMMARY.md`
</output>
