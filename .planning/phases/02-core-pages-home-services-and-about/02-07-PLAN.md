---
phase: 02-core-pages-home-services-and-about
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/sections/ServicesSection.tsx
  - src/components/sections/WorkPreviewSection.tsx
  - src/components/sections/TeamStrip.tsx
  - src/components/sections/AboutSection.tsx
autonomous: true

must_haves:
  truths:
    - "A condensed services teaser (name, one-line summary, warranty highlight only — not full per-service depth) exists as a standalone, reusable section component"
    - "A condensed team strip (photo-or-initials + name + title only, no long-form bio) exists as a standalone, reusable section component"
    - "A Work-preview teaser section exists that honestly reflects no real project data exists yet (no fabricated project photos/names), linking to /projects"
  artifacts:
    - path: "src/components/sections/ServicesSection.tsx"
      provides: "Condensed services section, maps SERVICES from content/services.ts"
      min_lines: 25
    - path: "src/components/sections/TeamStrip.tsx"
      provides: "Condensed team strip (sibling to TeamGrid, not a modification of it)"
      min_lines: 25
    - path: "src/components/sections/AboutSection.tsx"
      provides: "Condensed About section: brand-story copy (kept in full) + TeamStrip"
      min_lines: 15
    - path: "src/components/sections/WorkPreviewSection.tsx"
      provides: "Work preview teaser linking to /projects"
      min_lines: 15
  key_links:
    - from: "src/components/sections/ServicesSection.tsx"
      to: "src/content/services.ts"
      via: "import { SERVICES } from \"@/content/services\""
      pattern: "from \"@/content/services\""
    - from: "src/components/sections/AboutSection.tsx"
      to: "src/components/sections/TeamStrip.tsx"
      via: "component composition"
      pattern: "TeamStrip"
    - from: "src/components/sections/WorkPreviewSection.tsx"
      to: "src/app/(marketing)/projects/page.tsx"
      via: "Button as={Link} href=\"/projects\""
      pattern: "href=\"/projects\""
---

<objective>
Build the four new condensed section components the single-page homepage needs (Services, Work preview, About, and a new TeamStrip), reusing `content/services.ts` and `content/team.ts` as-is per `02-CONTEXT.md` — presentation-only work, no data model changes.

Purpose: These are pure, self-contained presentational components with no routing or nav dependencies, so they can be built in parallel with 02-06's route surgery. Wave 2 (02-08) composes them into the actual `/` page.
Output: `ServicesSection.tsx`, `WorkPreviewSection.tsx`, `TeamStrip.tsx`, and `AboutSection.tsx` in `src/components/sections/`, each server-renderable, each following the existing content-model-first pattern (`02-04-SUMMARY.md`: typed content array → presentational section component).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-core-pages-home-services-and-about/02-CONTEXT.md
@.planning/phases/02-core-pages-home-services-and-about/02-RESEARCH.md
@src/content/services.ts
@src/content/team.ts
@src/components/sections/TeamGrid.tsx
@src/components/sections/TrustBadges.tsx
@src/components/motion/TextReveal.tsx
@src/components/motion/Reveal.tsx
@src/components/ui/Typography.tsx
@src/components/ui/Button.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: ServicesSection (condensed) and WorkPreviewSection</name>
  <files>src/components/sections/ServicesSection.tsx, src/components/sections/WorkPreviewSection.tsx</files>
  <action>
Create `src/components/sections/ServicesSection.tsx`:
- A server component (no `"use client"`) rendering a `<div>` (the caller wraps it in the `<section id="services">` — this component is the section *content*, not the `<section>` element itself, so it can be reused/tested independent of the page's id/anchor wiring).
- `TextReveal as="h2"` section heading (e.g. "What We Do").
- `.map()` over `SERVICES` from `@/content/services`, one condensed card per service showing only `name`, `summary`, and `warranty.label` (NOT `description` or `warranty.detail` — those stay reserved for Phase 3 case studies per `02-CONTEXT.md`). Each card wrapped in `Reveal` with an incrementing `delay` (e.g. `index * 0.05`) for a staggered feel, matching the sequenced-reveal pattern from `02-02-SUMMARY.md`.
- Responsive grid (`grid-cols-1 sm:grid-cols-2 lg:grid-cols-3` or similar, matching `TeamGrid`'s existing breakpoint convention) — no accordion/expand-in-place (02-CONTEXT.md leaves this as discretion; static teaser cards are simplest and sufficient).
- Use `Typography` for card `name`/`summary`, reuse `TrustBadges` for the `warranty.label` (pass `{ label: service.warranty.label, detail: "" }` is wrong — instead, since `TrustBadges` currently expects `{ label, detail }` and always renders both, either add an optional-`detail` variant to `TrustBadges` (rendering nothing when `detail` is empty/omitted) or render the warranty label directly with `Typography variant="body-sm"` instead of reusing `TrustBadges` — prefer the latter (simpler, avoids modifying a component built for a different context) unless `TrustBadges` already supports an optional `detail`.

Create `src/components/sections/WorkPreviewSection.tsx`:
- Server component, `TextReveal as="h2"` heading (e.g. "Recent Work").
- Since no real project data model exists yet (Phase 3 scope — `content/projects.ts` does not exist), do NOT fabricate placeholder project cards/photos. Instead render an honest teaser: one `Reveal`-wrapped paragraph explaining the project archive is being finalized (same honesty precedent as the original `/work` placeholder copy), plus a `Button as={Link} href="/projects" variant="secondary"` "View all Projects" CTA wrapped in `MagneticButton`.
- Document this decision in a code comment: full curated preview cards are Phase 3's responsibility once real project content exists.
  </action>
  <verify>`npx tsc --noEmit` passes. Both components render with no client-only hooks at the top level (server components), confirmed by absence of `"use client"` in either file.</verify>
  <done>`ServicesSection` renders all 7 services from `SERVICES` with name/summary/warranty-label only (verified by grep: file does not reference `.description` or `.warranty.detail`). `WorkPreviewSection` renders an honest teaser (no fabricated project data) with a working `/projects` link.</done>
</task>

<task type="auto">
  <name>Task 2: TeamStrip (condensed) and AboutSection</name>
  <files>src/components/sections/TeamStrip.tsx, src/components/sections/AboutSection.tsx</files>
  <action>
Create `src/components/sections/TeamStrip.tsx` as a NEW sibling component to `TeamGrid` (do not modify `TeamGrid.tsx` itself — it may still be useful later per `02-04-SUMMARY.md`'s own reasoning):
- Same props shape as `TeamGrid` (`{ members: TeamMember[] }`), imported from `@/content/team`.
- Reuse the exact same `initialsFor()` neutral-placeholder-tile logic and "real photo via `next/image` OR neutral `bg-ink`/initials tile, never stock imagery" contract — copy/adapt this logic into `TeamStrip.tsx` (small enough that a shared-util extraction isn't warranted for 3 team members; if it feels like more than ~10 lines of duplication, extract `initialsFor` into `src/lib/utils.ts` instead and import it from both `TeamGrid` and `TeamStrip`).
- Render ONLY `name` and `role` beneath each photo/placeholder tile — no `bio` paragraph (the explicit "no long-form bio text per person" condensation from `02-CONTEXT.md`).
- Tighter horizontal-strip layout (e.g. `flex flex-wrap gap-8` or a denser grid than `TeamGrid`'s `sm:grid-cols-2 lg:grid-cols-3`), smaller photo/placeholder tiles than `TeamGrid`'s full-size cards.

Create `src/components/sections/AboutSection.tsx`:
- Server component, `TextReveal as="h2"` heading (e.g. "About Stoneage Properties").
- The brand-story copy (30+ years combined experience, JCT contracts, Solihull/London/Nottingham offices) kept in FULL per `02-CONTEXT.md` — do not shorten this part. Reuse/adapt the existing `/about` page's narrative paragraphs (read from git history via `git show HEAD:src/app/\(marketing\)/about/page.tsx` if the file was already deleted by 02-06's concurrent/prior execution — the text should not be rewritten from scratch, it already exists and was approved in 02-04).
- Followed by `<TeamStrip members={TEAM} />`.
  </action>
  <verify>`npx tsc --noEmit` passes. `grep -n "bio" src/components/sections/TeamStrip.tsx` returns no matches (bio text is intentionally excluded). No stock-photo domain strings (unsplash/pexels/stockphoto/placeholder.com) appear in either new file.</verify>
  <done>`TeamStrip` renders all `TEAM` members as photo-or-initials + name + role only, using the same neutral-placeholder contract as `TeamGrid`. `AboutSection` composes the full brand-story narrative with `TeamStrip`, ready to be dropped into the single page in Wave 2.</done>
</task>

</tasks>

<verification>
`npx tsc --noEmit` passes across all four new files. `npm run lint` (if configured) passes. None of the four components import routing-specific code beyond `next/link` (no dependency on `02-06`'s route changes — these are purely presentational and composable into any page).
</verification>

<success_criteria>
All four condensed section components exist, compile cleanly, follow the established content-model-first pattern (typed array → presentational component), and are ready for Wave 2 to compose into `/` without further content or accessibility rework.
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-pages-home-services-and-about/02-07-SUMMARY.md`
</output>
