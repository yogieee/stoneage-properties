---
phase: 02-core-pages-home-services-and-about
plan: 10
type: execute
wave: 3
depends_on: ["02-11", "02-12", "02-13", "02-14", "02-15", "02-16", "02-17"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "Zero stuck-opacity or overlapping-text states occur anywhere on the homepage under fast/slow/reversed scroll or hard-refresh-under-throttling — the three original 02-10 defects are gone, not just less frequent"
    - "The homepage reads as a photo-led, Storey-referenced experience: full-viewport hero carousel, 3-column photo-led Services teaser, decorative dividers, Testimonials, condensed About, in that order"
    - "Nav shows logo + in-page anchors + Projects + Services + Contact (as a button), with Contact still opening the sliding modal with the full brief-intake field set"
    - "/services is a real, reachable dedicated route with full per-service depth; /work, /about still redirect correctly (no dead 404s)"
    - "Everything renders correctly across mobile, tablet, and desktop breakpoints, and prefers-reduced-motion correctly disables all auto-cycling carousels and reveal animations sitewide"
  artifacts: []
  key_links: []
---

<objective>
Human-verify the second Phase 2 re-plan end-to-end — supersedes the previously-rejected 02-10 checkpoint (never approved, no SUMMARY.md exists for the prior version of this plan). This is the phase-level integration checkpoint confirming both problem classes from the 02-10 rejection are resolved: the root-caused animation defects (02-11) and the Storey-template-replication design direction (02-12 through 02-17).

Purpose: Plans 02-11 through 02-17 each verify their own artifacts in isolation. This plan confirms the pieces read as one coherent, premium, Storey-referenced experience together, on real viewports, under real stress-scroll conditions — and explicitly re-verifies the exact three symptoms the user reported at the prior rejected checkpoint, not just generic "looks fine" verification, per `02-CONTEXT.md`'s continuous-QA philosophy applied at the final integration point.
Output: User confirmation that the second-pivot Phase 2 build satisfies the ROADMAP.md Phase 2 success criteria (as interpreted for the hybrid single-page + dedicated `/services` structure) and specifically resolves the 02-10 rejection's two categories of findings; any issues found are logged as follow-up gap-closure work, not silently accepted.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-pages-home-services-and-about/02-CONTEXT.md
@.planning/phases/02-core-pages-home-services-and-about/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated pre-checks before human verification</name>
  <files></files>
  <action>
Before handing off to the user, run and confirm automated checks pass so the human checkpoint is spent on genuine visual/UX/animation-quality judgment, not catching build errors:
- `npm run build` — must succeed. Static route list must include `/`, `/projects`, `/services`, `/services/[slug]` (7 generated params), `/contact`, `/style-guide`, and must NOT include `/work`, `/about` as real pages (they should remain redirects per `next.config.ts`).
- `npx tsc --noEmit` — must pass with zero errors.
- `grep -n "/services" next.config.ts` — must show no `/services` → `/#services` redirect entry remains (02-14 removed it); `grep -n "/about" next.config.ts` must still show the `/about` → `/#about` entry (unchanged, About stays homepage-only).
- `grep -rn "onSplit" src/components/motion/TextReveal.tsx` and `grep -rn "fonts.ready" src/components/motion/SmoothScrollProvider.tsx` — both must return matches (02-11's fix is present, not accidentally reverted by a later plan).
- Start the dev server (`npm run dev`) and leave it running for the human-verify step below.
  </action>
  <verify>Build output and typecheck both succeed with no errors; the route list and redirect config match the expected second-pivot structure; the 02-11 fix markers are present in the codebase.</verify>
  <done>All Phase 2 routes build correctly under the second-pivot structure; the animation-fix markers are confirmed present; dev server is running and ready for manual verification.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
The second Phase 2 re-plan: root-caused `ScrollTrigger`/`SplitText` fixes (02-11) underlying every animated section; a full-viewport hero image carousel (02-13); a reintroduced `/services` route with full per-service depth plus a 3-column photo-led homepage teaser (02-14); a Storey-style nav with Contact rendered as a button and a new Services link (02-15); decorative section dividers and a Testimonials carousel (02-16); photo-led Work preview and About sections (02-17); and a fully field-expanded (still non-functional) contact modal/page (02-12).
  </what-built>
  <how-to-verify>
With the dev server running (`npm run dev`, default http://localhost:3000):

**A. Re-verify the three original 02-10 defects are gone (stress-scroll, not just normal scroll):**
1. Hard-refresh `/` several times in a row, including with devtools Network throttling set to "Slow 3G" or similar (to visibly delay webfont loading past first paint) — confirm the hero headline never renders overlapping/duplicated word spans, on any refresh.
2. Scroll down through Services and About at a fast, continuous scroll speed — confirm every card/paragraph reaches full opacity, none stay stuck partially transparent.
3. Scroll down past Services and About slowly, then scroll back UP past them, then down again (direction reversal) — confirm the same full-opacity result holds; nothing gets stuck in an intermediate state from the reversal.
4. Resize the browser window mid-scroll (e.g. drag narrower while a section is mid-reveal) — confirm no elements get stuck or misaligned.

**B. Re-verify the Storey-replication design direction:**
5. Scroll `/` top to bottom — confirm: full-viewport hero carousel (background image crossfades every few seconds, headline/CTAs legible on top), decorative dividers visible at each major section boundary, a 3-column photo-led Services teaser (each card has an image), a photo-led Work preview, a photo-led About section (with its own ambient image, distinct from the small TeamStrip photo/initials tiles below it), a Testimonials carousel cycling a few quotes, then the closing Contact CTA strip.
6. On the hero carousel, confirm a visible pause/play control exists and functions (click it, cycling stops/resumes).
7. Click "View All Services" (or equivalent) from the homepage teaser — confirm it lands on a real `/services` index page listing all 7 services; click into any one service — confirm its detail page shows a fuller description and warranty detail than the homepage teaser showed.
8. Confirm the nav shows: logo/home link, in-page anchors, "Services" (a real route link), "Projects", and "Contact" rendered as a visually distinct button (not plain text) — clicking Contact still opens the sliding modal (URL becomes `/contact`, no full page reload).
9. Open the Contact modal and confirm it now shows the full field set: name, email, project type (a dropdown populated with real service names), location (Solihull HQ/London/Nottingham), timeline, and message — with no working submit action. Open `http://localhost:3000/contact` directly in a fresh tab (hard navigation) and confirm the same field set appears on the standalone page.
10. Visit `http://localhost:3000/work` and `http://localhost:3000/about` directly — confirm both still redirect correctly (to `/projects` and `/#about` respectively) with no dead 404s.

**C. Cross-cutting checks:**
11. Using devtools device toolbar (or window resizing), check `/`, `/services`, `/services/[any-slug]`, and `/projects` at mobile (~375px), tablet (~768px), and desktop (~1440px) widths — confirm no layout breakage, overlapping text, unusable CTAs, or broken nav at any size.
12. Toggle OS/browser "reduce motion" (or emulate `prefers-reduced-motion: reduce` in devtools Rendering tab) and reload `/` — confirm: all TextReveal/Reveal content appears instantly with no animation, the hero carousel and Testimonials carousel both show a single static slide/quote with no auto-cycling (their pause controls may remain visually present per WCAG 2.2.2 but nothing should be moving on its own), and magnetic hover no longer follows the cursor on any CTA.

Report back either "approved" or a list of specific issues (section/page + breakpoint + what's wrong).
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to address</resume-signal>
</task>

</tasks>

<verification>
All ROADMAP.md Phase 2 success criteria, as re-scoped for the second-pivot hybrid structure by `02-CONTEXT.md`, confirmed true by the user against the live dev server under real stress-scroll conditions — not just inferred from passing builds. This checkpoint explicitly and separately re-verifies the exact three symptoms from the original 02-10 rejection, not just generic visual approval, before Phase 2 is considered complete.
</verification>

<success_criteria>
User has explicitly approved: (1) all three original 02-10 animation defects confirmed gone under stress-scroll conditions, and (2) the full Storey-replication design direction (hero carousel, photo-led Services teaser + dedicated route, nav chrome, dividers, testimonials, photo-led Work/About, expanded contact form fields) — across responsive breakpoints and reduced-motion behavior. Any issues raised are captured for a gap-closure plan rather than left unaddressed.
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-pages-home-services-and-about/02-10-SUMMARY.md`
</output>
