---
phase: 02-core-pages-home-services-and-about
plan: 04
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/content/team.ts
  - src/components/sections/TeamGrid.tsx
  - src/app/(marketing)/about/page.tsx
autonomous: true

must_haves:
  truths:
    - "Visitor can view an About/team page featuring the company's 30+ years combined experience story"
    - "Visitor sees a team grid that is structurally photo-ready (renders a real photo when one is supplied) but never shows stock imagery as a substitute"
  artifacts:
    - path: "src/content/team.ts"
      provides: "Typed team member array (name, role, bio, optional photo path) — photo-ready but not requiring a photo to render correctly"
      contains: "export const TEAM"
    - path: "src/components/sections/TeamGrid.tsx"
      provides: "Team member grid rendering a real next/image when photo is present, and a neutral monochrome initials tile (never stock photography) when absent"
      min_lines: 25
    - path: "src/app/(marketing)/about/page.tsx"
      provides: "About page with 30+ years story heading/copy and TeamGrid"
      contains: "TeamGrid"
  key_links:
    - from: "src/app/(marketing)/about/page.tsx"
      to: "src/components/sections/TeamGrid.tsx"
      via: "import { TeamGrid } from \"@/components/sections/TeamGrid\""
      pattern: "TeamGrid"
    - from: "src/components/sections/TeamGrid.tsx"
      to: "src/content/team.ts"
      via: "receives TEAM entries as props or imports directly"
      pattern: "photo"
---

<objective>
Build the About/team page (ABOUT-01): the 30+ years combined experience story plus a team grid, explicitly designed around the known content gap that real team photography does not yet exist as a project asset.

Purpose: 02-RESEARCH.md (Pitfall 3, Open Question 2) and STATE.md both flag that ABOUT-01 requires real team photos with "no stock imagery," but no real photos exist yet. The correct engineering response is to make `TeamGrid`/`content/team.ts` fully photo-ready (renders correctly with a real photo path the moment the client supplies one) while the fallback is a neutral, on-brand monochrome placeholder — never stock photography. This plan ships the page structurally complete; final real-photo sign-off on ABOUT-01 remains a content dependency outside this codebase, tracked explicitly below rather than silently assumed away.

**Content dependency (flagged, not blocking):** Real team member names/roles/bios/photos have not been supplied by the client as of this plan. `content/team.ts` should be populated with the real team structure/count if known from PROJECT.md; if individual member details are not yet available, use the minimum viable real detail known (e.g. company-level role types) and clearly mark any entry using placeholder identity data with a `// TODO: replace with real client-supplied bio` comment — never fabricate a person or use a stock photo URL.

Output: `content/team.ts`, `components/sections/TeamGrid.tsx`, `app/(marketing)/about/page.tsx`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-core-pages-home-services-and-about/02-RESEARCH.md
@src/components/ui/Typography.tsx
@src/components/motion/Reveal.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Team content + photo-ready TeamGrid with neutral placeholder</name>
  <files>src/content/team.ts, src/components/sections/TeamGrid.tsx</files>
  <action>
Create `src/content/team.ts`:
```typescript
export type TeamMember = {
  name: string;
  role: string;
  bio: string;
  photo?: string; // optional — path under /public, e.g. "/team/jane-doe.jpg"
};
export const TEAM: TeamMember[] = [ /* real entries where known, per Objective's content-dependency note */ ];
```
Check PROJECT.md for any real team/founder names, roles, or the "30+ years combined experience" narrative detail already captured during discovery — use exactly that if present. Do not invent fabricated named individuals if PROJECT.md has none; if no real names exist yet, it is acceptable to populate `TEAM` with role-based placeholders (e.g. `{ name: "Founding Director", role: "...", bio: "..." }`) explicitly commented as pending real client data — this keeps the grid structurally real (not empty) without inventing false identities.

Create `src/components/sections/TeamGrid.tsx`:
- Props: `members: TeamMember[]`.
- Renders a responsive grid (`grid-cols-1 sm:grid-cols-2 lg:grid-cols-3` or similar, satisfying ENG-05).
- Per member: if `photo` is present, render via `next/image` with proper `sizes`/`alt={member.name}`; if absent, render a neutral monochrome placeholder tile — a square `bg-paper-dim` (or `bg-ink` for contrast) div containing the member's initials in `font-display`, using only existing semantic tokens. **Never** use a stock photo URL (e.g. unsplash/pexels) as the fallback — this is an explicit, testable constraint from ABOUT-01.
- Name/role/bio rendered below each tile using `Typography` primitives.
  </action>
  <verify>`npx tsc --noEmit` passes. Grep the new files for any external image-hosting domain string (`unsplash`, `pexels`, `stockphoto`, `placeholder.com` image URLs) — must return no matches. Render `TeamGrid` with a mix of members with/without `photo` — confirm both states render correctly (real image vs. initials tile), no broken image icon.</verify>
  <done>`TeamGrid` never renders a stock photo under any circumstance (verified by grep), correctly displays a real photo when `photo` is set, and displays a neutral on-brand initials placeholder when it is not — both states are visually complete, not broken/half-rendered.</done>
</task>

<task type="auto">
  <name>Task 2: About page composition</name>
  <files>src/app/(marketing)/about/page.tsx</files>
  <action>
Create `src/app/(marketing)/about/page.tsx`:
- `TextReveal as="h1"` page heading (e.g. "30+ Years of Combined Experience").
- `Reveal`-wrapped narrative paragraph(s) using the real "30+ years combined experience... value for money, quick communication, professional expertise" story already established in the homepage skeleton/PROJECT.md — do not restate it verbatim identically to the homepage hero copy; write it as the fuller About-page version of the same real facts.
- Render `<TeamGrid members={TEAM} />` from `content/team.ts` below the narrative section.
- Server component, no unnecessary `"use client"` at the page level (motion primitives already isolate client code).
  </action>
  <verify>`npx tsc --noEmit` passes, `npm run build` succeeds with `/about` as a static route. View-source on `/about` confirms narrative text and team member names/roles are present in SSR HTML (ENG-04 carryover).</verify>
  <done>`/about` renders the 30+ years story and a complete, responsive TeamGrid; all text content is server-rendered and visible without JS execution.</done>
</task>

</tasks>

<verification>
`npm run build` succeeds with `/about` listed as a static route. Grep `src/content/team.ts` and `src/components/sections/TeamGrid.tsx` for stock-photo domain strings — zero matches. Resize the About page in devtools across mobile/tablet/desktop breakpoints — TeamGrid reflows correctly at each.
</verification>

<success_criteria>
ABOUT-01's structural requirements (30+ years story, team grid, no stock imagery) are fully met; the only remaining gap is real client-supplied photography/bios, which is explicitly documented in this plan and in STATE.md as a content dependency, not silently glossed over.
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-pages-home-services-and-about/02-04-SUMMARY.md`
</output>
