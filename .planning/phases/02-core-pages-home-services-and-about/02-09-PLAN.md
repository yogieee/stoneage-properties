---
phase: 02-core-pages-home-services-and-about
plan: 09
type: execute
wave: 3
depends_on: ["02-08"]
files_modified:
  - src/app/(marketing)/layout.tsx
  - src/app/(marketing)/contact/page.tsx
  - src/app/(marketing)/@modal/default.tsx
  - src/app/(marketing)/@modal/(.)contact/page.tsx
  - src/components/sections/ContactModal.tsx
autonomous: true

must_haves:
  truths:
    - "Clicking 'Contact' in the nav, or any 'Enquire' CTA, opens a bottom-to-top sliding modal over the current page, and the URL updates to /contact"
    - "Clicking outside the modal panel dismisses it and returns to the previous page/URL"
    - "Visiting /contact directly (hard navigation, e.g. typed URL or page refresh) renders a real standalone contact page, not a broken/404 modal state"
  artifacts:
    - path: "src/app/(marketing)/@modal/(.)contact/page.tsx"
      provides: "Intercepted route rendering ContactModal over the current page on client-side navigation"
      min_lines: 10
    - path: "src/app/(marketing)/@modal/default.tsx"
      provides: "Required fallback returning null for the unmatched @modal slot (prevents 404 on refresh, per 02-RESEARCH.md Pitfall 1)"
      contains: "return null"
    - path: "src/components/sections/ContactModal.tsx"
      provides: "Slide-up-from-bottom modal shell: GSAP enter/exit animation, outside-click dismiss, router.back() close, focus handling"
      min_lines: 40
    - path: "src/app/(marketing)/contact/page.tsx"
      provides: "Real standalone /contact fallback page for hard navigation"
      min_lines: 10
  key_links:
    - from: "src/app/(marketing)/@modal/(.)contact/page.tsx"
      to: "src/components/sections/ContactModal.tsx"
      via: "component composition"
      pattern: "ContactModal"
    - from: "src/components/sections/ContactModal.tsx"
      to: "next/navigation"
      via: "router.back() on outside-click/close"
      pattern: "router.back()"
    - from: "src/app/(marketing)/layout.tsx"
      to: "src/app/(marketing)/@modal/default.tsx"
      via: "modal prop rendered alongside children"
      pattern: "modal"
---

<objective>
Build the sliding contact modal using Next.js parallel routes (`@modal` slot) + intercepting routes (`(.)contact`), per `02-CONTEXT.md`'s explicit requirement and `02-RESEARCH.md` Topic 2 — a modal reachable from the nav "Contact" link and any "Enquire" CTA, with a real shareable `/contact` URL, dismissed by clicking outside it.

Purpose: This is the officially-documented Next.js pattern for "modal with a URL" — not something to hand-build with `usePathname` + client state (02-RESEARCH.md's "Don't Hand-Roll" table). This plan builds the modal SHELL and its motion/interaction mechanics only — NOT the full CONT-01 lead-capture form (name/email/project-type/location/timeline/message fields, office selection). That form logic is explicit Phase 4 scope per `02-CONTEXT.md`'s Impact section ("Phase 4's scope shifts from 'build the contact page' to 'build the contact modal + its form logic'"). This plan's modal renders placeholder contact content (heading, brief copy, direct contact details already used in the site footer) so the modal mechanism is fully working and verifiable now, without inventing form fields Phase 4 hasn't been planned for yet.
Output: `app/(marketing)/@modal/default.tsx`, `app/(marketing)/@modal/(.)contact/page.tsx`, `app/(marketing)/contact/page.tsx` (real fallback), `components/sections/ContactModal.tsx` (the shared modal shell both the intercepted and fallback routes render), `layout.tsx` updated to accept and render the `modal` slot.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-core-pages-home-services-and-about/02-CONTEXT.md
@.planning/phases/02-core-pages-home-services-and-about/02-RESEARCH.md
@src/app/(marketing)/layout.tsx
@src/components/motion/Reveal.tsx
@src/components/motion/TextReveal.tsx
@src/lib/gsap.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: ContactModal shell — slide-up animation, outside-click dismiss, a11y baseline</name>
  <files>src/components/sections/ContactModal.tsx</files>
  <action>
Create `src/components/sections/ContactModal.tsx` (`"use client"`) as a plain ref-based `useGSAP()` component — Option B from `02-RESEARCH.md` Pitfall 2 (custom `<div>`, not native `<dialog>`), chosen for consistency with this codebase's existing `Reveal`/`TextReveal`/`MagneticButton` pattern (every other animated element here is a plain ref + `useGSAP`, and Option B gives full control over the outside-click requirement without fighting native `<dialog>` backdrop semantics):

- Structure: a full-viewport backdrop `<div>` (fixed inset-0, semi-transparent `bg-ink/40` or similar using existing semantic tokens) behind a bottom-anchored panel `<div>` (`role="dialog"` `aria-modal="true"` `aria-labelledby` pointing at an internal heading id), panel positioned `fixed inset-x-0 bottom-0` with rounded top corners, containing: a `TextReveal` or `Typography` heading ("Get in Touch" or similar), a short paragraph, and the same office/contact details already in the site footer (`enquiries@stoneageproperties.com`, `0121 537 8229`, Solihull HQ / London / Nottingham) — this is placeholder content standing in for Phase 4's real form, not a fabricated form.
- Enter animation: `useGSAP()` timeline animating the panel from `y: "100%"` to `y: "0%"` on mount (`gsap.from`), respecting `gsap.matchMedia()` reduced-motion gating exactly like `Reveal`/`TextReveal` (instant final position under `prefers-reduced-motion: reduce`, no animation).
- Close behavior: accept an `onClose: () => void` prop (the caller — the route page — supplies `router.back()`). Wire close to: (a) clicking the backdrop `<div>` (not the panel itself — use a `mousedown`/`pointerdown` listener on the backdrop only, verifying `event.target === event.currentTarget` so clicks inside the panel don't bubble-trigger close), (b) an `Escape` keydown listener on `document` while mounted, cleaned up on unmount.
- Focus handling: on mount, move focus to the heading (`tabIndex={-1}` + `.focus()` in a `useEffect`/`useGSAP` callback). Manual focus trap: query all focusable descendants of the panel on mount, wrap `Tab` from the last back to the first and `Shift+Tab` from the first to the last via a `keydown` listener scoped to the panel.
  </action>
  <verify>`npx tsc --noEmit` passes. Manually mount `ContactModal` via the route in Task 2 — confirm it slides up from the bottom on open, backdrop click closes it, clicking inside the panel does NOT close it, `Escape` closes it, and Tab cycles only within the panel's focusable elements.</verify>
  <done>`ContactModal` is a self-contained, reduced-motion-aware slide-up panel with outside-click/Escape dismissal, focus moved to it on open and trapped within it while open, calling the caller-supplied `onClose` rather than managing navigation itself.</done>
</task>

<task type="auto">
  <name>Task 2: Parallel + intercepting routes wiring for /contact</name>
  <files>src/app/(marketing)/layout.tsx, src/app/(marketing)/contact/page.tsx, src/app/(marketing)/@modal/default.tsx, src/app/(marketing)/@modal/(.)contact/page.tsx</files>
  <action>
Create `src/app/(marketing)/@modal/default.tsx`: a server component returning `null` — REQUIRED per `02-RESEARCH.md` Pitfall 1, without this, refreshing `/` (or any route) after the modal was previously opened in-session produces a 404 for the unmatched `@modal` slot. Do this FIRST before wiring the intercepted route content.

Create `src/app/(marketing)/@modal/(.)contact/page.tsx`: a client component (or a thin server wrapper rendering a client child) that renders `<ContactModal onClose={() => router.back()} />`, using `useRouter` from `next/navigation`. The `(.)` intercepts `contact` at the same segment level as `@modal` (both live directly under `app/(marketing)/`), matching `02-RESEARCH.md` Topic 2's documented folder structure.

Create `src/app/(marketing)/contact/page.tsx`: the REAL full-page fallback for hard navigation (typed URL, refresh, shared link) — a server component rendering the page inline (not through the modal mechanism) with the same heading/copy/contact-details content as `ContactModal`'s placeholder content, but as a normal in-flow page section (reuse `TextReveal`/`Reveal` directly here, not `ContactModal` — a hard-loaded page should not render a fixed-position slide-up panel with no page behind it to slide over).

Update `src/app/(marketing)/layout.tsx`: add a `modal: React.ReactNode` prop alongside `children`, and render `{modal}` alongside `{children}` inside the existing `<main>`/layout structure (per `02-RESEARCH.md` Topic 2 — `@modal` is passed to the layout as a prop, not resolved as a URL segment).
  </action>
  <verify>Dev server: (a) from `/`, click "Contact" in the nav — modal slides up, URL becomes `/contact`, backdrop-click returns to `/` with the modal gone and URL back to `/`. (b) Directly navigate to `http://localhost:3000/contact` (hard load/refresh) — a real full standalone contact page renders, no modal/backdrop artifact. (c) Refresh `/` after having previously opened the modal in the same session — no 404.</verify>
  <done>Client-side navigation to `/contact` (nav link or any Enquire CTA) opens `ContactModal` over the current page with a real URL change and clean `router.back()` dismissal; hard navigation to `/contact` renders the standalone fallback page; `@modal/default.tsx` prevents the documented refresh-404 failure mode.</done>
</task>

</tasks>

<verification>
`npm run build` succeeds (parallel/intercepting route conventions are build-time validated by Next.js — a missing `default.tsx` or malformed `(.)` folder name fails the build, not just runtime). `npx tsc --noEmit` passes. Manual verification per Task 2's `<verify>` covers both interception paths (soft nav vs hard nav) explicitly, since this is the one behavior static analysis cannot confirm.
</verification>

<success_criteria>
The contact modal is fully wired per `02-CONTEXT.md`'s requirement (slide up from bottom, outside-click dismiss, reachable via nav "Contact" and any "Enquire" CTA, real shareable `/contact` URL) using the documented Next.js parallel/intercepting-routes pattern, with placeholder contact content standing in for Phase 4's not-yet-planned lead-capture form.
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-pages-home-services-and-about/02-09-SUMMARY.md`
</output>
