---
phase: 02-core-pages-home-services-and-about
plan: 15
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/sections/SiteNav.tsx
autonomous: true

must_haves:
  truths:
    - "The nav's Contact item renders as a visually distinct filled/outlined button, not a plain text link, while still triggering the existing sliding contact modal (not a full page navigation)"
    - "The nav includes a real link to the reintroduced /services route, alongside the existing in-page anchors and Projects/Contact links"
  artifacts:
    - path: "src/components/sections/SiteNav.tsx"
      provides: "Nav with Button-styled Contact link and a Services route link"
      contains: "Button"
  key_links:
    - from: "src/components/sections/SiteNav.tsx"
      to: "src/components/ui/Button.tsx"
      via: "Button as={Link} href=\"/contact\""
      pattern: "as={Link}"
---

<objective>
Rework the global nav's "Contact" item into a visually distinct button (matching storeyarchitecture.co.uk's nav chrome: logo left, links center-right, Contact as a filled/outlined button) using the project's existing `Button as={Link}` polymorphic pattern, and add a nav link to the reintroduced `/services` route, per `02-CONTEXT.md`'s navigation-chrome decision.

Purpose: `02-RESEARCH.md` Topic 5 confirms this is a pure styling change — Next.js's intercepting-route behavior for `/contact` triggers on any client-side `<Link>` navigation regardless of visual presentation, and this exact `Button as={Link}` composition is already proven working elsewhere in this codebase (`Hero.tsx`'s "Enquire" CTA). No routing risk, no new pattern.
Output: `SiteNav.tsx` with "Contact" rendered via `Button as={Link} href="/contact"`, and a new "Services" nav item linking to `/services` (a real route again after `02-14`), added alongside the existing logo/anchor-links/Projects structure.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-core-pages-home-services-and-about/02-CONTEXT.md
@.planning/phases/02-core-pages-home-services-and-about/02-RESEARCH.md
@src/components/sections/SiteNav.tsx
@src/components/ui/Button.tsx
@src/components/sections/Hero.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Contact-as-button + Services nav link</name>
  <files>src/components/sections/SiteNav.tsx</files>
  <action>
In `SiteNav.tsx`:
- Replace the current plain `<Link href="/contact" className="text-ink-muted hover:text-ink">Contact</Link>` with `<Button as={Link} href="/contact" variant="primary">Contact</Button>` (import `Button` from `@/components/ui/Button`) — matching `Hero.tsx`'s already-working `Button as={Link} href="/contact" variant="primary"` "Enquire" composition exactly. Do NOT wrap it in `MagneticButton` (per `02-RESEARCH.md` Topic 5: Storey's nav button doesn't have a magnetic-follow effect; plain `Button as={Link}` is the closer visual match) unless it visually reads better wrapped — Claude's discretion, default to unwrapped.
- Add a new nav item linking to `/services` (`<Link href="/services" className="text-ink-muted hover:text-ink">Services</Link>`, matching the existing "Projects" link's styling — a real route link, not an anchor, since `/services` is a dedicated page again per `02-14`), positioned logically among the existing items (e.g. between the in-page anchors and "Projects", or wherever reads best given the existing order: logo, Services anchor (`/#services`), Work anchor, About anchor, Projects, Contact — note the existing in-page "Services" anchor link (`/#services`, pointing at the homepage teaser section) is DIFFERENT from this new "Services" route link (`/services`, the full page) — do not conflate or remove the existing anchor link; both can coexist, but consider whether keeping both reads as duplicate/confusing in the nav and, if so, keep only the anchor link removed in favor of the real route link since `/services` now offers strictly more (the homepage teaser is still reachable by scrolling, without needing a dedicated nav item for it) — Claude's discretion, document the choice made.
- Verify the `.is-active` scroll-spy styling and existing `ScrollTrigger.create()` wiring for the remaining anchor links (`work`, `about`, and `services` if kept) still functions correctly after this change — the scroll-spy `useGSAP()` effect and its `ANCHOR_LINKS` array should only reference anchors that still exist in the rendered nav.
  </action>
  <verify>`npx tsc --noEmit` passes. Manually verify via dev server: the nav's "Contact" item renders as a filled button (not plain text), clicking it still opens the sliding contact modal (not a full page reload) with the URL becoming `/contact`; a "Services" link is present and navigates to the real `/services` page; no console errors about `ScrollTrigger` targeting a removed anchor.</verify>
  <done>The nav's Contact item is a real button (visually and via the `Button` component) that still triggers the existing intercepting-route modal; a working link to `/services` exists in the nav; scroll-spy highlighting for the remaining anchor links still functions with no console errors.</done>
</task>

</tasks>

<verification>
`npx tsc --noEmit` passes. Manual verification confirms the Contact button still opens the modal (not a full navigation), the Services link reaches the real route, and scroll-spy highlighting is unaffected for any anchor links that remain.
</verification>

<success_criteria>
The nav visually matches the Storey reference's Contact-as-button treatment and correctly exposes the reintroduced `/services` route, with zero regression to the existing modal-trigger or scroll-spy mechanics.
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-pages-home-services-and-about/02-15-SUMMARY.md`
</output>
