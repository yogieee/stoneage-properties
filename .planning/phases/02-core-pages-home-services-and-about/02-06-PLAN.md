---
phase: 02-core-pages-home-services-and-about
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - next.config.ts
  - src/app/(marketing)/work/page.tsx (renamed to src/app/(marketing)/projects/page.tsx)
  - src/app/(marketing)/services/page.tsx (deleted)
  - src/app/(marketing)/services/[slug]/page.tsx (deleted)
  - src/app/(marketing)/about/page.tsx (deleted)
  - src/components/sections/Hero.tsx
autonomous: true

must_haves:
  truths:
    - "Visiting /projects shows the (renamed) placeholder project-archive page; visiting /work redirects (308) to /projects"
    - "Visiting /services or /about no longer 404s silently — both redirect to the corresponding single-page anchor on /"
    - "The homepage Hero's 'View Projects' CTA links directly to /projects, not /work"
  artifacts:
    - path: "next.config.ts"
      provides: "Permanent redirects: /work -> /projects, /services -> /#services, /about -> /#about"
      contains: "redirects()"
    - path: "src/app/(marketing)/projects/page.tsx"
      provides: "Renamed placeholder project-archive route (content unchanged from the old /work placeholder), still Phase 3's to replace"
      min_lines: 15
  key_links:
    - from: "src/components/sections/Hero.tsx"
      to: "src/app/(marketing)/projects/page.tsx"
      via: "Button as={Link} href=\"/projects\""
      pattern: "href=\"/projects\""
---

<objective>
Consolidate Phase 2's routing to match the single-page pivot: rename the `/work` placeholder to `/projects` (with a redirect), and remove the now-dropped dedicated `/services` and `/about` routes (with defensive redirects to their future on-page anchors), per `02-CONTEXT.md`.

Purpose: This is pure route surgery — no new UI is built here. It clears the ground so Wave 2 can compose the single scrolling page without stale routes competing for the same content, and gives any already-shared preview-deploy links to `/work`, `/services`, or `/about` a correct destination instead of a silent 404.
Output: `/projects` route (renamed from `/work`), `/work` → `/projects` 308 redirect, `/services` → `/#services` and `/about` → `/#about` 308 redirects, `Hero.tsx`'s "View Projects" CTA pointing at `/projects` directly.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-core-pages-home-services-and-about/02-CONTEXT.md
@.planning/phases/02-core-pages-home-services-and-about/02-RESEARCH.md
@src/app/(marketing)/work/page.tsx
@src/components/sections/Hero.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename /work to /projects and redirect the old path</name>
  <files>src/app/(marketing)/work/page.tsx, src/app/(marketing)/projects/page.tsx, next.config.ts, src/components/sections/Hero.tsx</files>
  <action>
Rename the folder `src/app/(marketing)/work/` to `src/app/(marketing)/projects/` (move `page.tsx` as-is — its placeholder content, comment, and "Enquire" CTA stay unchanged; only the route segment moves). Do not change the placeholder's copy in this task — Phase 3 still owns replacing it with the real filterable gallery.

Update `next.config.ts` to add a `redirects()` async function returning:
```typescript
{ source: "/work", destination: "/projects", permanent: true }
```

Update `src/components/sections/Hero.tsx`: change the "View Projects" CTA's `href="/work"` to `href="/projects"` so internal navigation never takes the redirect hop (Pitfall 3 in 02-RESEARCH.md).
  </action>
  <verify>`npm run build` lists `/projects` (not `/work`) as a static route. `grep -rn "\"/work\"" src/` returns no matches in JSX/href usage (the string may still appear in comments referencing "Phase 3 work" — only href/route usage must be gone).</verify>
  <done>`/projects` renders the same placeholder content the old `/work` route had; `/work` returns a 308 redirect to `/projects`; `Hero.tsx`'s "View Projects" CTA links straight to `/projects`.</done>
</task>

<task type="auto">
  <name>Task 2: Drop dedicated /services and /about routes with defensive redirects</name>
  <files>src/app/(marketing)/services/page.tsx, src/app/(marketing)/services/[slug]/page.tsx, src/app/(marketing)/about/page.tsx, next.config.ts</files>
  <action>
Delete `src/app/(marketing)/services/page.tsx`, `src/app/(marketing)/services/[slug]/page.tsx` (and the now-empty `services/` and `services/[slug]/` folders), and `src/app/(marketing)/about/page.tsx` (and the now-empty `about/` folder). Per `02-CONTEXT.md`, these routes are dropped entirely — the content they rendered becomes condensed sections on `/` in a later wave (built separately in 02-07/02-08), not rebuilt here.

Do NOT delete `src/content/services.ts`, `src/content/team.ts`, `src/components/sections/TrustBadges.tsx`, or `src/components/sections/TeamGrid.tsx` — these are reused by the condensed sections in 02-07.

Add two more entries to the same `redirects()` array in `next.config.ts` (alongside the `/work` entry from Task 1):
```typescript
{ source: "/services", destination: "/#services", permanent: true },
{ source: "/about", destination: "/#about", permanent: true },
```
These are defensive per 02-RESEARCH.md Open Question 1 (cheap insurance in case a preview deploy was already crawled/bookmarked) — the `#services`/`#about` anchor targets don't exist on `/` yet until 02-08 lands; that's expected and fine, the redirect still resolves correctly to `/` with the browser attempting to scroll to the anchor once it exists.
  </action>
  <verify>`npm run build` no longer lists `/services`, `/services/[slug]`, or `/about` as routes. `curl -sI http://localhost:3000/services` (dev server) shows a `308` with `location: /#services`; same pattern for `/about`.</verify>
  <done>Dedicated `/services` and `/about` routes no longer exist in the build output; both paths 308-redirect to their future single-page anchors; `content/services.ts`, `content/team.ts`, `TrustBadges`, and `TeamGrid` remain on disk untouched for reuse.</done>
</task>

</tasks>

<verification>
`npm run build` succeeds; static route list includes `/`, `/projects`, `/style-guide` and no longer includes `/work`, `/services`, `/services/[slug]`, or `/about`. `npx tsc --noEmit` passes.
</verification>

<success_criteria>
All stale multi-page routes from the pre-pivot Phase 2 plans are gone or redirected, `Hero.tsx` links directly to the renamed `/projects` route, and every reusable content/component asset (`content/services.ts`, `content/team.ts`, `TrustBadges`, `TeamGrid`) survives untouched for Wave 2 to consume.
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-pages-home-services-and-about/02-06-SUMMARY.md`
</output>
