---
phase: 01-foundation-architecture-design-system-and-content-layer
plan: 04
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified:
  - src/components/ui/Typography.tsx
  - src/components/ui/Button.tsx
  - src/app/(marketing)/style-guide/page.tsx
  - src/app/(marketing)/page.tsx
autonomous: false

must_haves:
  truths:
    - "Core marketing content in a skeleton page is visible in server-rendered HTML (view-source shows real text without executing JavaScript)"
    - "A demo/style-guide page displays the monochrome/neutral palette and bold editorial typography system consistently"
    - "Toggling the OS 'reduce motion' setting on a demo animated section removes or simplifies the animation (no retrofitted fallback)"
  artifacts:
    - path: "src/components/ui/Typography.tsx"
      provides: "Design-system typography primitives consuming @theme tokens (headings, body text variants)"
    - path: "src/components/ui/Button.tsx"
      provides: "Design-system button primitive with hover micro-interaction, consuming @theme tokens"
    - path: "src/app/(marketing)/style-guide/page.tsx"
      provides: "Living reference page proving the design-token system renders consistently (success criterion 2)"
    - path: "src/app/(marketing)/page.tsx"
      provides: "Skeleton homepage: Server Component rendering real hardcoded Stoneage copy in server HTML (success criterion 1), wrapped in Reveal (success criterion 3)"
  key_links:
    - from: "src/app/(marketing)/page.tsx"
      to: "src/components/motion/Reveal.tsx"
      via: "wraps SSR'd content section, motion is a leaf client boundary"
      pattern: "<Reveal"
    - from: "src/app/(marketing)/style-guide/page.tsx"
      to: "src/components/ui/Typography.tsx"
      via: "renders all design-token variants for visual audit"
      pattern: "Typography"
---

<objective>
Build the design-system UI primitives and the style-guide/skeleton-homepage pages that prove Phase 1's three success criteria simultaneously, on real hardcoded content and real motion primitives — then verify each criterion explicitly with the user.

Purpose: This is where Phase 1's systems (design tokens, motion, SSR-by-default architecture) get composed together for the first time and proven correct as a whole, not just in isolation. This plan is the phase's actual goal-backward target: everything built in Plans 01-01 and 01-02 exists to make this plan's verification pass.

Note: A CMS was originally planned for Phase 1 (Sanity), built in a since-reverted Plan 01-03, then descoped from v1 entirely — the client wants a self-built AWS/Terraform backend, which is real infra work deferred to v2 (see PROJECT.md Key Decisions, superseded 2026-09-08). This plan uses hardcoded content instead; there is no CMS success criterion.

Output: A style-guide page auditing the design system, a skeleton homepage proving SSR + reduced-motion-safe animation together on real copy, and an explicit human verification pass against all three Phase 1 success criteria.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/PROJECT.md
@.planning/phases/01-foundation-architecture-design-system-and-content-layer/01-RESEARCH.md
@.planning/phases/01-foundation-architecture-design-system-and-content-layer/01-01-SUMMARY.md
@.planning/phases/01-foundation-architecture-design-system-and-content-layer/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build design-system UI primitives and the style-guide page</name>
  <files>src/components/ui/Typography.tsx, src/components/ui/Button.tsx, src/app/(marketing)/style-guide/page.tsx</files>
  <action>
    Build `src/components/ui/Typography.tsx` — a small set of typed components (or a single polymorphic component with variants) consuming the `@theme` tokens from Plan 01 (`--text-display-xl`, `--text-body`, etc.) via Tailwind utility classes generated from those tokens. No raw Tailwind default text-size/color utilities — only project semantic tokens (`text-ink`, `bg-paper`, etc., or whatever exact token names Plan 01's SUMMARY documents).

    Build `src/components/ui/Button.tsx` using `cn()` from `src/lib/utils.ts`, styled with theme tokens, with a hover micro-interaction (a CSS transition is acceptable here — MOTION-02's magnetic/hover interaction is full scope for Phase 2's real CTAs; Phase 1 only needs the primitive to exist and demonstrate the token system, not the final magnetic-cursor treatment).

    Build `src/app/(marketing)/style-guide/page.tsx` — a Server Component (no `"use client"` at the page level) that renders:
    - Color palette swatches for every token defined in `globals.css`'s `@theme` block
    - Every `Typography` variant at full size (proving the editorial type scale)
    - `Button` in its variants
    - At least one section wrapped in `<Reveal>` (from Plan 02) to double as this criterion's animated demo section for the reduced-motion test

    This page is explicitly a living reference/audit tool per research's Pitfall 3 guidance — every component built in later phases should visually match what's shown here.
  </action>
  <verify>`npm run dev`, load `/style-guide` — palette swatches, type scale, and buttons render using only theme tokens (`grep -n "text-gray-\|bg-slate-" src/app/(marketing)/style-guide/page.tsx src/components/ui/*.tsx` returns no matches). `curl -s http://localhost:3000/style-guide | grep -i "color\|typography"` confirms content is server-rendered.</verify>
  <done>`/style-guide` displays the full monochrome palette and editorial type scale consistently, using only project-defined semantic tokens, with at least one `Reveal`-wrapped section.</done>
</task>

<task type="auto">
  <name>Task 2: Build the skeleton homepage proving SSR + motion together on real hardcoded content</name>
  <files>src/app/(marketing)/page.tsx</files>
  <action>
    Rewrite `src/app/(marketing)/page.tsx` as a Server Component (no `"use client"`):

    ```typescript
    import { Reveal } from "@/components/motion/Reveal";

    export default function Home() {
      return (
        <main>
          <h1>Stoneage Properties — Specialist Contractors for Building Projects</h1>
          <Reveal>
            <p>
              Over 30 years of combined experience delivering new builds, renovations,
              extensions, and conversions across Solihull, London, and Nottingham —
              built on value for money, quick communication, and professional expertise.
            </p>
          </Reveal>
        </main>
      );
    }
    ```

    Use real Stoneage Properties tagline/copy from PROJECT.md's Context section (services, tagline, 30+ years experience) — this is real content, hardcoded directly in the codebase (no CMS, no database), which proves "core marketing content is server-rendered." Keep the `Reveal` wrapper scoped to its own leaf; the page itself must remain a Server Component throughout (no `"use client"` at the page or layout level).

    This is intentionally still a skeleton (full homepage design/layout is Phase 2 scope) — the only goal here is proving the SSR + motion architectural loop works on real copy.
  </action>
  <verify>`curl -s http://localhost:3000 | grep "Stoneage Properties"` returns the real heading text with JS disabled/unexecuted. View page with JS disabled in browser DevTools — heading and paragraph text are both visible.</verify>
  <done>`/` (homepage) is a Server Component rendering real static Stoneage copy, present in server-rendered HTML, with the paragraph wrapped in the `Reveal` motion primitive.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 1's complete foundation: Next.js 15.5 + Tailwind v4 design-token system, GSAP/Lenis motion primitives with reduced-motion built in, and a style-guide + skeleton homepage proving both systems work together on real hardcoded content (no CMS in v1 — deferred to v2).
  </what-built>
  <how-to-verify>
    Verify each of Phase 1's three success criteria explicitly:

    1. **SSR/indexability (ENG-04):** Run `curl -s http://localhost:3000 | grep "Stoneage Properties"` — or open `view-source:http://localhost:3000` in a browser — and confirm the homepage heading and paragraph text are present as real text, not empty divs.

    2. **Design system consistency (MOTION-03):** Visit `http://localhost:3000/style-guide` and confirm the monochrome/neutral palette and bold editorial typography render consistently across all swatches/variants shown.

    3. **Reduced motion (ENG-02):** With `http://localhost:3000/style-guide` (or `/`) open, toggle OS-level reduce-motion (macOS: System Settings -> Accessibility -> Display -> Reduce Motion; or Chrome DevTools -> Rendering tab -> "Emulate CSS media feature prefers-reduced-motion: reduce"), then reload the page. Confirm the `Reveal`-wrapped section appears instantly/statically with no scroll-triggered animation, and that page scrolling behaves as native 1:1 tracking rather than smoothed.
  </how-to-verify>
  <resume-signal>Type "approved" if all three criteria pass, or describe which criterion failed and what you observed instead.</resume-signal>
</task>

</tasks>

<verification>
All three Phase 1 success criteria (from ROADMAP.md) verified directly against the running app by the user, not just by automated checks:
1. Real marketing content visible in server-rendered HTML
2. Style-guide page displays the design system consistently
3. OS reduce-motion toggle demonstrably changes animated section behavior
</verification>

<success_criteria>
Phase 1 is complete: the technical foundation (Next.js/Tailwind scaffold, motion primitives, SSR-by-default architecture) is provably correct on real hardcoded content, verified by the user against all three ROADMAP.md success criteria, ready for Phase 2 to build real pages against it without re-litigating any of these foundational decisions.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-architecture-design-system-and-content-layer/01-04-SUMMARY.md`
</output>
